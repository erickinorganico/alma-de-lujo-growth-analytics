---
phase: 02-reconciled-decision-metrics
plan: 01
type: execute
wave: 1
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - alma/operating_mart_contracts.py
  - contracts/operating-metrics-policy-v1.schema.json
  - policies/operating-metrics-synthetic-v1.json
  - alma/operating_cost_inventory.py
  - models/operating_cost_inventory.sql
  - tests/test_operating_marts_cost_inventory.py
autonomous: true
requirements: [MET-01, MET-02, MET-07]
must_haves:
  truths:
    - "An owner can distinguish a known partial cost sum, documented complete cost, estimated complete cost, markup, gross margin, and contribution for the cost version effective on the measured date."
    - "An analyst can reconcile purchase, receipt, disposition, custody, reservation, loan, and available units without replayed physical movements."
    - "A missing Phase 1 input or incomplete source coverage blocks only dependent complete metrics and never converts unknown into zero."
  artifacts:
    - path: alma/operating_mart_contracts.py
      provides: "Bound Phase 1 relation map, metric and policy contracts, source/coverage validation"
    - path: contracts/operating-metrics-policy-v1.schema.json
      provides: "Versioned policy input schema distinct from immutable source facts"
    - path: policies/operating-metrics-synthetic-v1.json
      provides: "Explicit synthetic example policy with no owner approval claim"
    - path: alma/operating_cost_inventory.py
      provides: "Exact cost allocation and unit-conservation controls"
    - path: models/operating_cost_inventory.sql
      provides: "Preaggregated cost, purchase, receipt, and custody projections"
    - path: tests/test_operating_marts_cost_inventory.py
      provides: "Hand-calculated cent/unit oracles and negative controls"
  key_links:
    - from: alma/operating_mart_contracts.py
      to: "Phase 1 v1 schema and manifest"
      via: "explicit relation/column map verified against SQLite and source hashes"
    - from: alma/operating_cost_inventory.py
      to: models/operating_cost_inventory.sql
      via: "integer-grain projections before Decimal calculations"
---

<objective>
Build the shared Phase 1-to-mart contract and exact cost, purchasing, receipt, and inventory outputs for an immutable cut.

Purpose: Owners need comparable economics and trustworthy available units without duplicated cents or physical stock.
Output: A verified semantic adapter, read-only SQL/Python mart family, and a bounded synthetic test oracle.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reconciled-decision-metrics/02-CONTEXT.md
@.planning/phases/02-reconciled-decision-metrics/02-RESEARCH.md
@.planning/phases/01-versioned-operating-intake/01-CONTEXT.md
@.planning/phases/01-versioned-operating-intake/01-01-SUMMARY.md
@.planning/phases/01-versioned-operating-intake/01-02-SUMMARY.md
@.planning/phases/01-versioned-operating-intake/01-03-SUMMARY.md

<interfaces>
Bind to the final Phase 1 schema, not the proposed names in `01-RESEARCH.md`. The required semantic inputs are cut ID/hash, contract version, cutoff/timezone, source hashes, coverage by domain/window, canonical SKU and economic/physical event IDs, effective cost versions/components/allocations, prices and variable selling costs, purchase lines, receipt dispositions, stock movements/counts, reservations, loans, availability observations, unmet-demand observations, independent cash-balance evidence, and aggregate delivery-cohort linkage for return/quality events. Require an explicit source-to-field mapping and verify PK/FK, dates, units and coverage on the actual SQLite database. If Phase 1 lacks a required fact or linkage, fix the Phase 1 contract under its owning work before publishing that metric; an aggregate or count cannot be invented as an event.

Expose `MetricDefinition(id, version, formula, unit, grain, window, sources, unknown, guardrail, owner, decision_use)` and `MetricRow(metric_id, cut_id, as_of, dimensions, numerator, denominator, value, status, coverage_refs, source_refs, reconciliation_id)` from `alma/operating_mart_contracts.py`. Valid row statuses are `MEASURED`, `PARTIAL`, `UNKNOWN`, `NOT_APPLICABLE`, `ESTIMATED`, `ERROR`; a documented/estimated cost quality flag is separate. A distinct versioned policy artifact has `policy_version`, `effective_start`, `effective_end`, `status` (`REVIEW`, `APPROVED`, `SYNTHETIC_EXAMPLE`), `owner_approval_ref` when approved, explicit bases/thresholds/maturity windows, and SHA-256; metric rows carry policy version/hash/status separately from measurement status. Other Phase 2 families import these contracts. Use half-open local-date windows `[start, end)` and a new derived private output path; do not edit the accepted cut.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind the finished Phase 1 schema and shared metric contract</name>
  <files>alma/operating_mart_contracts.py, contracts/operating-metrics-policy-v1.schema.json, policies/operating-metrics-synthetic-v1.json, tests/test_operating_marts_cost_inventory.py</files>
  <behavior>Unknown/zero/partial are distinct; a mismatched source hash, absent required relation/column, duplicate canonical ID, unsupported timezone, or stale cut fails before mart publication; missing/stale/unapproved policy never becomes owner approved; synthetic policy never authorizes a real cut.</behavior>
  <action>After all Phase 1 summaries exist, inspect its final `alma/operating_contracts.py` exports, STRICT SQLite schema, manifest/verify functions, and synthetic pack. Record exact table/column/PK/FK mappings in `alma/operating_mart_contracts.py`; this is the mandatory schema-binding step for D-01 and D-03. Test that Phase 1 `SOURCE_NAMES` contains exactly the 22 D-03 sources (`sku_catalog`, `sales_aggregates`, `availability_daily`, `unmet_demand`, `inventory_counts`, `inventory_movements`, `inventory_reservations`, `cost_versions`, `cost_components`, `cost_allocations`, `purchase_orders`, `purchase_receipts`, `obligations`, `obligation_payments`, `cash_events`, `cash_balance_evidence`, `budgets`, `budget_allocations`, `expenses`, `quality_events`, `sales_readiness`, `loans`) and assert the semantic key/date/unit/coverage fields needed by all MET-01..07, including cash evidence and delivery-cohort links. Implement manifest verification, cut/as-of/coverage access, MetricDefinition/MetricRow validation, and a read-only connection. Create a separate JSON policy schema and synthetic example artifact with version, effective interval, bases/thresholds/maturity, approval status/ref, and canonical SHA-256; validate missing, stale, unapproved and synthetic policy states without changing the source cut. Use the real Phase 1 intake API for a two-cut temporary fixture. If a required Phase 1 field is absent, fail the binding test and reconcile the contract before declaring its dependent metric complete. Verify explicit Python 3.11+ and STRICT SQLite support.</action>
  <verify><automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe -m unittest discover -s tests -p test_operating_marts_cost_inventory.py -v</automated></verify>
  <done>All 22 Phase 1 source names and MET-01..07 semantic fields are bound and tested; invalid mappings and hashes fail before output; versioned policy validation separates measurement from approval and covers missing/stale/unapproved/synthetic cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize versioned cost and price economics</name>
  <files>alma/operating_cost_inventory.py, models/operating_cost_inventory.sql, tests/test_operating_marts_cost_inventory.py</files>
  <behavior>One 101-cent component within one SKU/cost version distributed to three stable unit ordinals yields 34, 34 and 33 cents, exactly 101; missing required cost yields known partial sum and unknown complete value; estimates remain labeled; historical economics retain the version effective on the event date; zero revenue or COGS denominator is not applicable.</behavior>
  <action>Implement MET-01 per D-02/D-03. SQL preaggregates distinct components and the Phase 1 allocation rows by their source/component and owning SKU/cost version; the Phase 1 contract permits no multi-SKU allocation of a component. Never join raw many-to-many allocations to prices or receipts, and use only integer `SUM`. Python distributes indivisible batch cents across stable unit ordinals within the same SKU/cost version, assigning residual cents deterministically; retain any unallocated source cents when basis is missing and reject overlapping approved effective versions or inclusion cycles. Publish known component sum separately from complete landed cost, with `COMPLETE_DOCUMENTED` and `COMPLETE_ESTIMATED` quality. Use the version effective on each measured date. For realized economics use eligible aggregate `net_revenue_cents` and delivered-unit COGS at the same SKU/channel/window; keep `public_price_cents` separate and derive discount only if recorded. Compute markup, gross margin and contribution with Decimal as specified in the metric definition. Read bases and inclusion rules from the bound versioned policy artifact; a real cut with missing/stale/unapproved policy remains `REVIEW` and cannot present fiscal profit.</action>
  <verify><automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe -m unittest discover -s tests -p test_operating_marts_cost_inventory.py -v</automated></verify>
  <done>Every cost source reconciles to allocated plus unallocated integer cents; complete cost and dependent margin/contribution appear only with eligible components and policy; missing/estimated/historical versions and zero denominators pass the hand oracle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile purchase, receipt, and stock custody once</name>
  <files>alma/operating_cost_inventory.py, models/operating_cost_inventory.sql, tests/test_operating_marts_cost_inventory.py</files>
  <behavior>A live 20-unit order with 18 received, 2 under inspection and 16 accepted has 2 still to receive; receipt dispositions use the Phase 1 accepted/inspection/rejected fields exactly; a receipt replay or return report without accepted restock adds zero stock; reserved and loaned units reduce proper available/custody buckets; unresolved count variance blocks affected available/valuation.</behavior>
  <action>Implement MET-02 per D-02/D-04. At purchase-order/as-of grain reconcile live ordered to received plus still-to-receive, with explicit cancelled states; at receipt-event grain enforce the Phase 1 identity `received_units = inspection_units + accepted_units + rejected_units` with no invented `accepted_non_sellable` source field. Derive non-sellable custody from `inventory_counts.non_sellable_units`, permitted `inventory_movements`/quality dispositions, and verified as-of reconciliation, marking it partial or unknown when those observations do not determine a complete state. Use unique movement/source IDs so physical receipt plus accepted movement is one stock entry, and return reports add no sellable units until an allowed `RETURN_RESTOCK` movement. Counts are variance observations, never additive receipts. Publish owned, sellable, inspection, non-sellable, loaned, reserved and available units only at eligible coverage; `available = sellable_on_hand - active_reserved` must be nonnegative. Reject duplicate/replayed events and impossible negatives; unresolved variance or missing movement coverage blocks affected available/valuation while unrelated metrics remain available.</action>
  <verify><automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe -m unittest discover -s tests -p test_operating_marts_cost_inventory.py -v</automated></verify>
  <done>All receipt and custody conservation equations hold by canonical event and as-of; no duplicate or reported-only return inflates stock; reservations, loans, variance and missing coverage produce the declared statuses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 1 cut to derived marts | Verify manifest/hash, canonical IDs, coverage, and relation map before reading private facts. |
| Derived marts to private output | Write only under ignored private root, using a new versioned bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | cut and event IDs | mitigate | Hash verification plus PK/source-event uniqueness and replay negative controls in Task 1/3. |
| T-02-02 | Information disclosure | derived cost output | mitigate | Private-root path guard and no public export in Task 1. |
| T-02-03 | Repudiation | allocation and reconciliation | mitigate | Record source IDs, deterministic residual rows and reconciliation IDs in Task 2. |
</threat_model>

<verification>
Run the focused unittest module on a temporary synthetic Phase 1 cut. Assert exact cents/units, source conservation, unknown propagation, stable historical versions, hash mismatch rejection, and no published artifact on failed controls. The final integrated mart check runs after all three plans.
</verification>

<success_criteria>
MET-01 and MET-02 produce exact as-of outputs tied to verified Phase 1 sources; their definitions meet MET-07; all stated negative controls pass without changing the accepted workspace.
</success_criteria>

<output>
Create `.planning/phases/02-reconciled-decision-metrics/02-01-SUMMARY.md` when done.
</output>
