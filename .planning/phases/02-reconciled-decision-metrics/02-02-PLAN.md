---
phase: 02-reconciled-decision-metrics
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - alma/operating_finance_marts.py
  - models/operating_finance_marts.sql
  - tests/test_operating_marts_finance.py
autonomous: true
requirements: [MET-03, MET-05, MET-07]
must_haves:
  truths:
    - "An owner sees one obligation balance with applied payments, including a distinct partial state when application coverage is incomplete."
    - "An owner sees settled/reconciled cash, committed, expected, undated, and scenario layers separately for the same 56-day and 91-day cut windows."
    - "Approved budget, open commitment, incurred, paid, outstanding, and unallocated amounts reconcile by drop/channel without treating a payment as new spend."
  artifacts:
    - path: alma/operating_finance_marts.py
      provides: "Decimal-safe obligation, cash, budget controls and metric definitions"
    - path: models/operating_finance_marts.sql
      provides: "Source-grain integer aggregation of payments, cash events, and allocations"
    - path: tests/test_operating_marts_finance.py
      provides: "Hand-computed obligations, cash horizon, and allocation negative controls"
  key_links:
    - from: alma/operating_finance_marts.py
      to: alma/operating_mart_contracts.py
      via: "verified Phase 1 relation map, coverage, and MetricRow/MetricDefinition"
    - from: alma/operating_finance_marts.py
      to: models/operating_finance_marts.sql
      via: "preaggregated economic-event IDs before any drop/channel joins"
---

<objective>
Materialize canonical obligation/payment balances, layered cash, and budget/drop/channel financial states for a verified Phase 1 cut.

Purpose: A payment, commitment, expense, or scenario must never appear as a second liability, incurred cost, or reconciled cash event.
Output: Read-only finance SQL/Python marts and exact synthetic controls.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reconciled-decision-metrics/02-CONTEXT.md
@.planning/phases/02-reconciled-decision-metrics/02-RESEARCH.md
@.planning/phases/01-versioned-operating-intake/01-CONTEXT.md
@.planning/phases/02-reconciled-decision-metrics/02-01-SUMMARY.md

<interfaces>
Import Phase 2 `MetricDefinition`, `MetricRow`, verified cut reader, bound Phase 1 relation map, half-open local-date windows, status rules and versioned policy validator from `alma/operating_mart_contracts.py`. Phase 1's `01-CONTEXT.md` specifies `obligations.csv`, `obligation_payments.csv`, `cash_events.csv`, `cash_balance_evidence.csv`, `budgets.csv`, `budget_allocations.csv`, and `expenses.csv`; require canonical obligation/payment/origin IDs, due/settlement/expected dates, source hashes and coverage, independent opening/closing amounts with observed dates/evidence status, budget ceiling, expense/commitment state, and target drop/channel allocation plus unallocated amount. Bind to actual SQLite table/column names from 02-01; never use CSV names as an unverified runtime schema. An absent linkage blocks its dependent reconciliation, not a guessed join.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile canonical obligations and applied payments</name>
  <files>alma/operating_finance_marts.py, models/operating_finance_marts.sql, tests/test_operating_marts_finance.py</files>
  <behavior>An obligation of 180000 cents with 100000 uniquely applied and no adjustment has 80000 recorded unpaid; duplicate applications fail; a payment with incomplete application coverage leaves authoritative outstanding PARTIAL; an undated obligation remains undated.</behavior>
  <action>Implement MET-03 per D-03/D-04 at `obligation_id × as_of`. Preaggregate distinct settled application IDs and documented adjustments at their own grain before joining. Calculate `recorded_unpaid_cents = original_cents - documented_adjustments_cents - recorded_applied_cents`; enforce nonnegative adjusted original, `0 <= applied <= adjusted original`, and one canonical obligation per origin/economic event. Keep PO, invoice, budget commitment and expense as lineage/state references, never additional liabilities. Show recorded amount under partial coverage but mark authoritative outstanding `PARTIAL`; include due date or `undated` bucket, source refs, cut hash and reconciliation ID. Export family metric definitions meeting MET-07.</action>
  <verify><automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe -m unittest discover -s tests -p test_operating_marts_finance.py -v</automated></verify>
  <done>The 180000/100000/80000 oracle and coverage states pass; replayed/over-applied payment IDs and duplicate obligation origins fail before publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Produce layered actual and forecast cash in both horizons</name>
  <files>alma/operating_finance_marts.py, models/operating_finance_marts.sql, tests/test_operating_marts_finance.py</files>
  <behavior>A settled payment supersedes its committed outflow for the same as-of and never appears in both layers; absent independent opening/closing evidence leaves close UNKNOWN while cash movements remain visible; day 56 is outside the eight-week window but inside the thirteen-week window; undated and unapproved scenario events remain separate.</behavior>
  <action>Implement MET-03 per D-02/D-03/D-04. Require Phase 1 `economic_event_id`, source ID, layer, event date, settlement/due/expected date, scenario ID and supersession link for each cash projection. `reconciled_close = verified_opening + settled inflows - settled outflows ± verified adjustments` only when opening and independent closing evidence agree; authorization/capture is not settlement. Assign each active economic event once by as-of precedence: settled actual supersedes committed or expected forecast; dated outstanding obligations feed committed, selected expectations feed expected, undated exposure stays undated, and scenario deltas stay outside baseline until selected. Compute daily closes and week buckets for half-open 56-day and 91-day windows from the same facts, preserving local timezone and reporting daily minimum when available. Read projection selection and floor from the bound policy artifact with its own hash/status; missing or stale policy blocks the dependent forecast, and an unapproved real cut remains `REVIEW`. Use integer cents and Decimal ratios.</action>
  <verify><automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe -m unittest discover -s tests -p test_operating_marts_finance.py -v</automated></verify>
  <done>Reconciled/committed/expected/undated/scenario components and 8/13-week boundaries are distinct; no event is double counted; unknown opening cannot yield a reconciled balance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile budget, drop, and channel financial states</name>
  <files>alma/operating_finance_marts.py, models/operating_finance_marts.sql, tests/test_operating_marts_finance.py</files>
  <behavior>One 101-cent expense allocated across two targets has target shares plus explicit unallocated equal to 101; paying a 60-cent incurred expense does not make incurred 120; a source with two targets and two payment rows is counted once; missing target mapping remains unallocated.</behavior>
  <action>Implement MET-05 per D-02/D-04. At `budget_version × drop/channel × window`, expose separate `approved_ceiling_cents`, `open_commitment_cents`, `incurred_cents`, `paid_cents`, `outstanding_obligation_cents`, and `unallocated_cents`. Link PO/expense/obligation/payment through one economic source ID and mutually exclusive state mapping. Calculate `headroom = approved ceiling - (open commitment + allocated incurred)` only under the applicable versioned policy and complete mapped coverage; missing/stale policy blocks headroom and unapproved real policy marks it `REVIEW`. Paid is settlement, not additive exposure. Preaggregate each source and payment before target joins. Use Phase 1 explicit `budget_allocations` as source shares, then verify `sum(target shares)+unallocated=source cents`; assign any policy-permitted residual by stable target key without manufacturing a new source allocation. Missing drop/channel mapping retains unallocated cents and leaves unrelated total actuals visible. Export MET-07 definitions with policy hash/status in row provenance.</action>
  <verify><automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe -m unittest discover -s tests -p test_operating_marts_finance.py -v</automated></verify>
  <done>Approved, committed, incurred, paid, outstanding and unallocated values remain distinct; the source-to-target bridge reconciles to cents; join multiplication, ambiguous origin and additive payments fail controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 1 finance sources to derived projections | Validate cut/hash, economic IDs, source coverage, and dates before summing. |
| Forecast layer to owner-facing evidence | Preserve scenario/expected/unknown labels so projections cannot be mistaken for reconciled cash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | payment and allocation IDs | mitigate | Unique application/source controls and two-target/two-payment negative test in Tasks 1/3. |
| T-02-05 | Information disclosure | cash/budget output | mitigate | Use verified private output boundary from 02-01 and no sales-facing finance projection. |
| T-02-06 | Repudiation | cash-layer supersession | mitigate | Record source, economic event, replacement link, policy version and reconciliation ID in Task 2. |
</threat_model>

<verification>
Run the focused finance test module against temporary synthetic cuts. Verify source-to-target cents, event precedence, independent opening/closing evidence, unknown/partial coverage, 56/91-day boundaries, and publication failure on duplicate or ambiguous economic identities.
</verification>

<success_criteria>
MET-03 and MET-05 provide exact balances and cash/budget projections with complete MET-07 definitions, explicit source-state precedence, and no double-counted cents or false reconciled balances.
</success_criteria>

<output>
Create `.planning/phases/02-reconciled-decision-metrics/02-02-SUMMARY.md` when done.
</output>
