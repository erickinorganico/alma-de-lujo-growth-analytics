---
phase: 02-reconciled-decision-metrics
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - alma/operating_learning_exceptions.py
  - models/operating_learning_exceptions.sql
  - alma/operating_marts.py
  - tests/test_operating_marts_learning.py
autonomous: true
requirements: [MET-04, MET-06, MET-07]
must_haves:
  truths:
    - "An owner sees launch sell-through, variant mix, mature return/quality rates, observed stockout exposure, and recorded unmet demand with denominators, windows, and coverage."
    - "Unavailable variants, immature/unlinked return cohorts, and absent unmet-demand logging cannot produce false preference or zero-demand claims."
    - "Readiness, quality, and loan-custody exceptions identify an owner, next action, due date and closure evidence; the sales-facing projection discloses no internal cost or finance field."
    - "All Phase 2 metric definitions and results validate together; missing/stale policy, unsafe output paths, or failed reconciliation publish no derived bundle."
  artifacts:
    - path: alma/operating_learning_exceptions.py
      provides: "Cohort/exposure measures and governed action exceptions"
    - path: models/operating_learning_exceptions.sql
      provides: "Preaggregated sales, return, quality, availability, demand, readiness and loan facts"
    - path: alma/operating_marts.py
      provides: "Verified read-only mart build, registry validation and private atomic publication"
    - path: tests/test_operating_marts_learning.py
      provides: "Cohort, exposure, privacy and all-family publication controls"
  key_links:
    - from: alma/operating_learning_exceptions.py
      to: alma/operating_mart_contracts.py
      via: "Phase 1 source and coverage mapping plus MetricRow/MetricDefinition"
    - from: alma/operating_marts.py
      to: "alma/operating_cost_inventory.py and alma/operating_finance_marts.py"
      via: "collect family builders and register every emitted metric"
    - from: alma/operating_marts.py
      to: ".local/operating-marts/<cut-id>/<contract-hash>/"
      via: "resolve private root, verify cut and policy hashes, reconcile, stage, then atomically publish"
    - from: alma/operating_marts.py
      to: contracts/operating-metrics-policy-v1.schema.json
      via: "explicit policy_path validation and separate policy hash/status"
---

<objective>
Materialize coverage-aware product learning and actionable exception evidence, then publish the complete Phase 2 metric catalog and private derived mart bundle.

Purpose: Product and operations decisions need comparable exposure and cohort evidence while sensitive cost and finance facts remain private.
Output: Read-only learning/exception marts, a restricted sales projection, a validated metric registry, and an atomic private bundle.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reconciled-decision-metrics/02-CONTEXT.md
@.planning/phases/02-reconciled-decision-metrics/02-RESEARCH.md
@.planning/phases/01-versioned-operating-intake/01-CONTEXT.md
@.planning/phases/02-reconciled-decision-metrics/02-01-SUMMARY.md
@.planning/phases/02-reconciled-decision-metrics/02-02-SUMMARY.md

<interfaces>
Use the actual Phase 1 relation map and verified coverage/hash functions from `alma/operating_mart_contracts.py`. Phase 1's `01-CONTEXT.md` specifies `sales_aggregates.csv` and `quality_events.csv` with optional aggregate `delivery_cohort_id`, plus `availability_daily.csv`, `unmet_demand.csv`, `sales_readiness.csv`, `loans.csv`, movements and receipts. Require canonical SKU/variant and drop/channel keys; dated aggregate delivered/returned/restocked units; opening sellable and accepted receipts; linked physical returns and quality inspections; variant/day stock observations; recorded qualified unmet-demand rows and source-wide `unmet_demand` coverage; dated readiness evidence and loan custody/return evidence. Phase 1 has no channel-level unmet-demand coverage or separate asset/rights source: report only recorded channel observations, never an inferred channel zero, and use only readiness codes that the source actually supplies. If a private cut lacks observation or cohort linkage, emit `UNKNOWN`/`PARTIAL` for that measure and identify the missing source; do not reconstruct orders, customers, or demand. Use metric family builders/definitions from `alma/operating_cost_inventory.py` and `alma/operating_finance_marts.py` for the integrated registry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calculate cohort, exposure, and recorded-demand measures</name>
  <files>alma/operating_learning_exceptions.py, models/operating_learning_exceptions.sql, tests/test_operating_marts_learning.py</files>
  <behavior>A stockout-exposed variant keeps observed sales and exposure but yields no weak-preference claim; zero eligible stock gives NOT_APPLICABLE sell-through; immature or unlinked aggregate returns give UNKNOWN mature rate; a recorded unmet-demand observation is a lower bound and an absent channel row never becomes measured zero even when source-wide coverage is COMPLETE.</behavior>
  <action>Implement MET-04 per D-03/D-05. At declared `SKU/variant × launch cohort × [start,end)` calculate `net_depleted_units = delivered units - accepted restocked return units` and `sell_through = net depleted / (eligible opening sellable + accepted receipts during window)`, retaining gross delivered, physical returns and restocks as separate facts. At `drop/channel × variant × comparable window`, calculate descriptive variant mix as variant eligible delivered / same-exposure-set eligible delivered, with availability and price comparability fields. At mature cohort grain calculate physically received returns / eligible delivered and confirmed defects / eligible inspected separately; read maturation from the versioned policy and require complete event coverage and `delivery_cohort_id` linkage. At variant/day grain compute observed stockout exposure from the Phase 1 observed/sellable/stockout minutes and roll to eligible days. Publish only recorded `unmet_demand` events by source/reason/channel as lower-bound observations, carrying source-wide coverage; do not require nonexistent channel-level coverage and never emit a measured zero for a channel without an explicit complete channel observation. Use Decimal ratios and zero-denominator `NOT_APPLICABLE`; missing source/denominator/cohort/exposure gives `UNKNOWN` or `PARTIAL`. A real cut with missing/stale/unapproved policy remains `REVIEW`; never assert hero-product success from synthetic or stockout-limited evidence.</action>
  <verify><automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe -m unittest discover -s tests -p test_operating_marts_learning.py -v</automated></verify>
  <done>Sell-through, mix, mature return/quality, availability and qualified unmet-demand rows have exact numerators, denominators, dates and coverage; all listed false-zero/false-preference negative controls pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit actionable exceptions and restricted sales readiness</name>
  <files>alma/operating_learning_exceptions.py, models/operating_learning_exceptions.sql, tests/test_operating_marts_learning.py</files>
  <behavior>An uninspected receipt, unresolved quality defect, blocked sales-readiness code, overdue loan, or missing custody receipt yields a stable exception; absent owner/due/closure evidence remains unresolved; a sales-facing export excludes internal-cost exception categories and rejects sensitive values injected into otherwise allowed fields.</behavior>
  <action>Implement MET-06 per D-03/D-06 at `exception_id × cut`. Derive deterministic IDs from category plus canonical entity/source event and cut; include category, SKU/drop/channel/event ref, severity, evidence ref/hash, owner role, next action, due date, closure status and closure evidence ref. Add rules for incomplete required cost as internal-only review, uninspected receipt, quality defect awaiting disposition, blocked/review sales-readiness code, overdue loan, and missing custody return evidence; do not claim a separate asset/rights source absent from Phase 1. Read owner/threshold rules from the versioned policy; missing owner, date or closure proof remains unresolved. Build a sales-facing projection with both an explicit row-category allowlist (`SALES_READINESS`, `QUALITY_HOLD`, `LOAN_RETURN_DUE` only when the selected issue codes are public-safe) and an exact field allowlist of SKU, public variant, readiness/issue code, owner role, next action code, due date and closure state. Reject unlisted categories/keys; validate allowed string values against bounded public-code patterns and test injected cost, supplier, bank, recipient and PII-like values in allowed fields. Keep internal exception detail private and never copy raw opaque recipient tokens to sales output.</action>
  <verify><automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe -m unittest discover -s tests -p test_operating_marts_learning.py -v</automated></verify>
  <done>Each required exception has a stable action/owner/date/closure contract or explicit unresolved missing evidence; sales output passes row-category, field and value allowlists, including injected-value negatives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register and publish the complete reconciled mart bundle</name>
  <files>alma/operating_marts.py, tests/test_operating_marts_learning.py</files>
  <behavior>Every emitted metric has one complete definition; a source hash change, failed reconciliation, invalid status, missing definition, unsafe output path, or missing/stale policy leaves no published bundle; a valid cut publishes reproducible source, policy and artifact hashes.</behavior>
  <action>Implement MET-07 per D-01/D-03/D-06/D-07. Build a read-only `build_operating_marts(cut_path, output_root, *, policy_path)` entry point that verifies the Phase 1 manifest/source hashes and the separate policy schema/version/effective date/status/SHA-256, invokes the three family builders, validates unique metric IDs and every `MetricDefinition`/`MetricRow`, and cross-checks source coverage and family reconciliation IDs. Record policy hash/status separately from cut and metric-contract hashes; an unapproved real-cut policy yields `REVIEW`, while missing/stale policy blocks dependent policy-calculated metrics. Canonicalize and resolve both supplied paths, reject traversal, symlink/junction and output roots outside the configured ignored `.local/operating-marts` private root, and test each rejection before writing. Stage JSON/CSV facts, registry, exceptions, restricted sales readiness, per-family controls, source/policy lineage and artifact SHA-256 under `.local/operating-marts/<cut-id>/<contract-hash>/`; atomically publish only after checks pass. Reject existing outputs unless byte-identical verification is supported. Test two synthetic cuts, tamper/partial/duplicate cases, and preserve Phase 1/v0.2/v0.3 paths.</action>
  <verify>
    <automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe -m unittest discover -s tests -p test_operating_marts_*.py -v</automated>
    <automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe -m unittest discover -s tests -v</automated>
    <automated>C:\Users\erick\.pyenv\pyenv-win\versions\3.11.7\python.exe scripts/verify_v2.py --output build/verification-v2-phase2</automated>
  </verify>
  <done>All MET-01..07 definitions and rows validate, the complete derived bundle is reproducible and private, failed controls publish nothing, and the two-cut synthetic case preserves history and source hashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 1 observed aggregates to learning | Require declared coverage, cohort linkage, maturity and exposure before rate claims. |
| Internal exceptions to sales-facing projection | Apply a fixed field allowlist and reject cost/finance/recipient leakage. |
| Derived bundle to Phase 3 | Hash and verify every artifact against one cut and metric-contract version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | metric bundle | mitigate | Verify source and artifact hashes; stage and publish only after all controls in Task 3. |
| T-02-08 | Information disclosure | sales-facing readiness | mitigate | Row-category, field and value allowlists with injected-value and opaque-recipient negatives in Task 2. |
| T-02-09 | Repudiation | exception closure | mitigate | Stable exception ID, source evidence, closure proof and unresolved state in Task 2. |
| T-02-10 | Denial of service | invalid aggregate sizes | mitigate | Bound integer sums/date windows and reject overflow or malformed coverage through shared contract before publication. |
| T-02-11 | Information disclosure | private output path | mitigate | Resolve root and reject traversal, symlink/junction and outside destinations before staging in Task 3. |
</threat_model>

<verification>
Run the focused three mart test modules with a synthetic two-cut fixture and a tampered-cut negative control; run the existing full `unittest` discovery and `scripts/verify_v2.py` after integration to protect released v0.2/v0.3 behavior. No real filled cut enters public tests or evidence.
</verification>

<success_criteria>
MET-04 and MET-06 deliver eligible product measures and action exceptions; MET-07 covers every family with complete definitions and status validation; a verified private bundle preserves all MET-01..07 controls for Phase 3.
</success_criteria>

<output>
Create `.planning/phases/02-reconciled-decision-metrics/02-03-SUMMARY.md` when done.
</output>
