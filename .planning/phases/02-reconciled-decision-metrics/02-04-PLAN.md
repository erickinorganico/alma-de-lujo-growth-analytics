---
phase: 02-reconciled-decision-metrics
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - alma/operating_cost_inventory.py
  - tests/test_operating_marts_cost_inventory.py
  - alma/operating_learning_exceptions.py
  - models/operating_learning_exceptions.sql
  - tests/test_operating_marts_learning.py
  - alma/operating_finance_marts.py
  - alma/operating_marts.py
  - tests/test_operating_marts_finance.py
  - tests/test_operating_marts_publication.py
autonomous: true
gap_closure: true
requirements: [MET-01, MET-02, MET-03, MET-04, MET-05, MET-06, MET-07]
must_haves:
  truths:
    - "COGS is additive across disjoint channel/date partitions, and estimated complete costs remain ESTIMATED in every dependent economic measure."
    - "Return and defect rates use only mature, linked, covered delivery cohorts; immature and unlinked observations remain visible as UNKNOWN."
    - "Phase 1-valid observed cash is accepted, and all five cash layers appear faithfully in normalized 56/91-day results."
    - "Valid empty or missing domains preserve unrelated metrics without invented event rows or false measured zero."
    - "Every decision measure published in families.json has a complete semantic definition and equivalent normalized representation; documented facts are explicitly distinguished."
    - "Existing unit conservation, obligation, policy, privacy and atomic-publication controls remain effective."
  artifacts:
    - path: alma/operating_cost_inventory.py
      provides: Canonical pre-filter cost ordinals and estimated economics quality
      contains: realized_economics
    - path: alma/operating_learning_exceptions.py
      provides: Cohort-grain maturity and eligible pooled learning with excluded evidence
      contains: project_learning
    - path: models/operating_learning_exceptions.sql
      provides: Denominator-linked quality selection without multiplying source events
      contains: delivery_cohort_id
    - path: alma/operating_finance_marts.py
      provides: Canonical observed cash evidence and complete finance definitions
      contains: FINANCE_DEFINITIONS
    - path: alma/operating_marts.py
      provides: Complete semantic catalog, normalized measures and empty-domain publication
      contains: build_operating_marts
    - path: tests/test_operating_marts_publication.py
      provides: End-to-end cash, empty-domain and semantic registry failure controls
  key_links:
    - from: alma/operating_cost_inventory.py
      to: sales_aggregates
      via: Canonical sales_date/sku_id/channel_code ordering before reporting filters
      pattern: sales_aggregates
    - from: alma/operating_learning_exceptions.py
      to: models/operating_learning_exceptions.sql
      via: Source-grain queries feeding delivery_cohort_id eligibility
      pattern: delivery_cohort_id
    - from: alma/operating_marts.py
      to: alma/operating_finance_marts.py
      via: Consume horizons.layers_cents plus undated_cents and scenario_cents
      pattern: layers_cents
    - from: alma/operating_marts.py
      to: families.json
      via: Validate semantic family paths against registry and normalized rows before atomic publication
      pattern: families
---

<objective>
Close all five blocker groups in 02-VERIFICATION.md: partition-dependent COGS/estimated quality, pooled cohort maturity, observed cash/layer serialization, empty-domain publication, and incomplete semantic definitions. Restore the Phase 2 goal of one exact, coverage-aware account of costs, units, cash, learning and exceptions per cut, per D-01 through D-07.

Purpose: Existing helper tests passed while consumer behavior failed. Regressions must exercise actual projections and published artifacts with the verifier's counterexamples.
Output: Focused corrections to existing SQL/Python projections and publication, failing-then-passing regression tests, and fresh focused/full/verify_v2 receipts. No Phase 1 source contract changes, external actions or shared planning-state edits belong to this plan.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reconciled-decision-metrics/02-CONTEXT.md
@.planning/phases/02-reconciled-decision-metrics/02-RESEARCH.md
@.planning/phases/02-reconciled-decision-metrics/02-VALIDATION.md
@.planning/phases/02-reconciled-decision-metrics/02-VERIFICATION.md
@.planning/phases/02-reconciled-decision-metrics/02-01-SUMMARY.md
@.planning/phases/02-reconciled-decision-metrics/02-02-SUMMARY.md
@.planning/phases/02-reconciled-decision-metrics/02-03-SUMMARY.md
@alma/operating_mart_contracts.py
@alma/operating_contracts.py
@alma/operating_workspace.py
@alma/operating_cost_inventory.py
@alma/operating_learning_exceptions.py
@models/operating_learning_exceptions.sql
@alma/operating_finance_marts.py
@models/operating_finance_marts.sql
@alma/operating_marts.py
@tests/test_operating_marts_cost_inventory.py
@tests/test_operating_marts_finance.py
@tests/test_operating_marts_learning.py
@policies/operating-metrics-synthetic-v1.json
</context>

## Execution boundaries and dependency graph

Discovery level 0: use the actual existing Python standard-library/SQLite/unittest interfaces; install no packages. The verified interpreter is `.venv/Scripts/python.exe`; check Python/SQLite versions before tests. Tests copy `client/source-packs/v1/synthetic` into temporary directories, adjust metadata/linked synthetic rows consistently, call `build_operating_workspace`, then `bind_cut` or `build_operating_marts`. Never patch an accepted workspace to make an end-to-end case pass. Small in-memory STRICT-schema oracles may supplement these paths.

Root cause: all five groups are execution misses against original decisions; helper-only checks and selected-row registry equality concealed consumer failures. This plan repairs those boundaries rather than changing policy or deferring requirements. Existing plans 02-01/02-02/02-03 own the complete baseline; this dependent plan preserves their verified behavior.

| Task | Needs | Creates | Checkpoint |
|---|---|---|---|
| 1 | Existing verified cut and cost interfaces | Stable canonical ordinal allocation and truthful economics quality | no |
| 2 | Existing learning queries, policy and cut | Cohort-specific and eligible pooled rates, preserving excluded observations | no |
| 3 | Tasks 1/2 outputs and existing finance/publication | Canonical cash, complete semantic registry, valid empty-domain bundles and final regression evidence | no |

Execute these three bounded repairs sequentially in wave 4; task 3 updates the existing bundle assertions in the learning test module after task 2. Target context cost is approximately 12%, 15%, 23%; no new subsystem is required. Restrict implementation to listed files. Do not edit ROADMAP.md, STATE.md, REQUIREMENTS.md, validation or verification reports; the orchestrator owns reconciliation of shared state. Preserve concurrent edits.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allocate COGS before report partitioning and propagate estimated quality</name>
  <files>alma/operating_cost_inventory.py, tests/test_operating_marts_cost_inventory.py</files>
  <behavior>
    - test_cogs_partition_invariance: one 101-cent complete component, quantity_basis=3, one delivered unit on each of 2026-09-15/16/17; realized_economics over [Sep15,Sep18) returns 101 COGS and three adjacent daily calls return [34,34,33], summing to 101 rather than [34,34,34].
    - Partition the same canonical sales across channels and dates; recombining disjoint results preserves cents. A later window includes prior canonical unit consumption; changed query/insertion order does not change assignment. Historical cost versions have independent deterministic ordinal sequences.
    - test_estimated_cost_propagates_to_economics: a real temporary Phase 1 build with complete sales/cost coverage and one ESTIMATED cost component produces project_cost.status=ESTIMATED and realized_economics.status=ESTIMATED, retaining exact COGS/contribution and Decimal ratios. Missing components still withhold complete economics.
  </behavior>
  <action>Write and run the new failing projection tests first. Per D-01/D-02/D-03, replace the per-call ordinal reset in realized_economics with allocation across canonical SKU sales in stable primary-key order (sales_date, sku_id, channel_code), selecting the effective cost version at each sale date and advancing a separate unit ordinal per SKU/version before channel/window filtering. Preserve the existing quantity_basis allocation convention, using prefix sums/integer divmod if needed rather than materializing unbounded units; never allocate a component across SKUs. Prior sales determine offsets even when excluded by the current report. Track quality of selected cost inputs: any estimated complete input makes dependent COGS/profit/contribution/markup/margins ESTIMATED; missing/partial coverage and real-policy authority remain stronger blockers. Keep existing function signatures and return fields so task 3 can serialize corrected values/statuses. Per D-04, leave inventory and receipt accounting unchanged and retain its existing negative controls.</action>
  <verify><automated>.venv/Scripts/python.exe -m unittest discover -s tests -p test_operating_marts_cost_inventory.py -v</automated></verify>
  <done>The verifier's 101-versus-102 counterexample now returns 101 for both combined and partitioned results; channel/date/version partitions conserve cents; estimated inputs never yield MEASURED dependent economics. Existing cost/inventory tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Calculate maturity and quality at delivery-cohort grain</name>
  <files>alma/operating_learning_exceptions.py, models/operating_learning_exceptions.sql, tests/test_operating_marts_learning.py</files>
  <behavior>
    - test_project_learning_mixed_cohort_maturity: 10 deliveries on 2026-09-15, 90 deliveries on 2026-10-20, one RETURN_RECEIVED on 2026-10-01 linked to the September cohort, as_of 2026-10-21, returns maturity 30 days. project_learning with end=2026-10-22 returns eligible mature aggregate numerator=1, denominator=10, value='0.1'; the recent cohort remains UNKNOWN, never a MEASURED 1/100 rate.
    - Add a linked cohort outside the selected delivery window with its own returns, inspections and rejections; none changes selected-cohort numerators or denominators. An unlinked event and an immature linked cohort remain explicit UNKNOWN observations.
    - A selected mature cohort with 10 inspected and 2 rejected yields defect rate '0.2'; outside-cohort inspected/rejected events do not dilute or inflate it. Test maturity boundary, repeated sales rows per cohort, reported-only returns, partial coverage and zero denominator through project_learning.
  </behavior>
  <action>Write failing runtime projection cases before changing the scalar helpers. Per D-03/D-05, group selected deliveries by delivery_cohort_id, retaining their canonical sales keys and all source references. For cohorts spanning sales dates, require every included delivery to reach the policy maturity interval (latest included delivery date is the conservative cohort maturity anchor); do not use the oldest sale to mature unrelated units. Restrict the SQL quality query through an EXISTS or distinct selected-cohort relation so a quality_event_id contributes once and outside-window cohorts cannot enter the selected denominator. Preserve unlinked/excluded observations separately with reason and UNKNOWN status. Calculate return and inspected/rejected measures per linked cohort, then pool only eligible mature complete cohorts with matching numerators/denominators. Keep the existing mature_returns and quality_defect_rate summaries as eligible aggregates; add explicit cohort rows/exclusion evidence and emit cohort metric rows with cohort IDs plus an explicit aggregate scope dimension for summary rows. Definitions must describe these grains and policy maturity accurately. Keep reported returns separate from physical receipts, and preserve coverage, dates, cut/source/policy lineage, exposure, unmet-demand limits and existing public exception allowlists per D-06/D-07.</action>
  <verify><automated>.venv/Scripts/python.exe -m unittest discover -s tests -p test_operating_marts_learning.py -v</automated></verify>
  <done>The exact mixed-cohort counterexample yields 1/10 over the eligible population, while immature/unlinked evidence stays UNKNOWN. Outside-cohort returns/inspections cannot contaminate either rate. Existing exposure, exception and privacy cases still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish canonical cash, empty domains and every defined decision measure</name>
  <files>alma/operating_finance_marts.py, alma/operating_marts.py, tests/test_operating_marts_finance.py, tests/test_operating_marts_learning.py, tests/test_operating_marts_publication.py</files>
  <behavior>
    - test_observed_cash_and_all_layers_publish: append a unique independently observed RECONCILED INFLOW of 100 cents with no obligation_id/payment_id, increase observed closing balance by 100, and build through real Phase 1 intake and the mart publisher. Phase 2 accepts the valid cut and reconciles exactly. Also cover independently evidenced outflow and reject missing evidence or invalid payable linkage.
    - Include nonzero committed and expected dated events with distinct economic IDs. For both horizons 56/91, assert normalized RECONCILED, COMMITTED, EXPECTED, UNDATED and SCENARIO amounts match nested family values exactly, with correct signs, statuses, window and event lineage. Preserve day-56/day-91 half-open boundaries, supersession, forecast isolation and independent balance evidence.
    - test_zero_unmet_domain_preserves_unrelated_metrics: keep the unmet CSV header, remove observations, set its metadata coverage ZERO, and perform real intake/build. Publication succeeds, recorded_unmet_units has no fabricated rows, catalog retains its definition, and unrelated cost/inventory/finance rows survive. Repeat with MISSING coverage and assert coverage differs without inventing a channel zero.
    - test_empty_domain_matrix: run ZERO and MISSING cases for sales_aggregates, budgets, obligation_payments and cash_events using internally consistent linked temporary packs. No empty scenario selection or missing registry-row error may abort unrelated families; record explicit domain coverage. Known unpaid exposure survives missing payments, authoritative outstanding does not; measured zero and unknown cash/sales remain distinct. Removing a physical CSV or violating Phase 1 FKs must still fail intake.
    - test_semantic_registry_covers_published_measures: traverse actual families.json, checking every declared decision measure's definition and normalized counterpart with equal value/status/grain/window/lineage. Remove a definition and its normalized rows together while leaving its family measure: publication must fail, proving the gate is independent of selected metric_rows IDs. Inject an unclassified family measure and require failure. Explicitly documented source/control facts need a fact classification, not an invented metric.
    - test_estimated_economics_serialization: read the final bundle from task 1's estimated-cost fixture; COGS, contribution, markup, gross margin and contribution margin rows preserve ESTIMATED with exact values/ratios. Missing values retain truthful statuses and ratio zero denominators are NOT_APPLICABLE.
  </behavior>
  <action>First add tests/test_operating_marts_publication.py with real disposable intake-to-bundle fixtures and the named failure cases, plus focused finance tests; run them RED. Per D-01/D-04, make active_cash_events honor the existing Phase 1 cash-evidence contract: require payment evidence for obligation-linked settlements, accept independently observed inflows/outflows already validated by Phase 1, and preserve source evidence, supersession and balance checks. In project_cash handle an empty cash source without inventing a scenario: use an unambiguous observed scenario when available, otherwise expose explicit empty-domain/UNKNOWN state and no fabricated event-grain row. Per D-02/D-03, serialize cash horizons from layers_cents['RECONCILED'/'COMMITTED'/'EXPECTED'] plus undated_cents/scenario_cents using canonical layer dimensions, source-specific coverage/quality and correct scenario/event references; do not promote expected/scenario forecasts to measured cash or missing sources to zero. Per D-03/D-05/D-06, change _validate to require definitions for emitted rows rather than identical sets, preserve invalid-status/hash/reconciliation/path failures, and publish domain coverage/empty reasons without synthetic channel/event rows; update the old BundleTests registry-equality assertion to this contract. Per MET-07 and D-07, add an explicit semantic family-path catalog in operating_marts.py, with metric entries linked to definitions and exact normalized rows, and individually documented source/control fact entries; unknown numeric/ratio/decision paths fail validation. Inventory all published families rather than only existing metric_rows: known/complete/unallocated cost and public price/unit costs; revenue/delivered/COGS/variable/gross/contribution/markup/margins; purchase/receipt and inventory states; original/applied/unpaid/outstanding obligation states; actual cash/close, all horizon layers, weekly layers, daily closes/minimum/floor; approved/committed/incurred/paid/outstanding/unallocated budget measures at target/source grains; learning numerator/denominator, cohort/aggregate rates, exposure and recorded-demand measures. Preserve independent grain, window, source hashes/refs, policy status, and metric-specific reconciliation identity for each normalized decision measure. Extend COST_DEFINITIONS/FINANCE_DEFINITIONS and registry assembly; retain LEARNING_DEFINITIONS from task 2. Document observation/threshold/boolean-control fields explicitly as facts where they are not derived decision metrics. Validate families against the catalog and normalized output before staging, including null/empty cases; catalog semantics participate in the metric-contract hash so changed meaning cannot reuse the old destination identity. Do not add internal measures to sales_readiness.json. Complete GREEN and run the focused, full and historical acceptance gates below on the final code.</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m unittest discover -s tests -p test_operating_marts_finance.py -v</automated>
    <automated>.venv/Scripts/python.exe -m unittest discover -s tests -p test_operating_marts_publication.py -v</automated>
    <automated>.venv/Scripts/python.exe -m unittest discover -s tests -p 'test_operating_marts_*.py' -v</automated>
    <automated>.venv/Scripts/python.exe -m unittest discover -s tests -v</automated>
    <automated>.venv/Scripts/python.exe scripts/verify_v2.py --output build/verification-v2-phase2-gap-closure</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>All five blocker groups are covered by passing production-path regressions. Every normalized value agrees with its published family measure; missing definitions or unknown semantic paths fail closed. Valid ZERO/MISSING domains publish unrelated results, no fake demand/event rows exist, and final focused/full/verify_v2 gates pass without modifying accepted cuts or historical evidence.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Accepted cut to calculations | Read-only Phase 1 source identity, coverage, cash evidence and cohort links must remain authoritative. |
| Family projections to publication | Missing catalog entries, omitted layers and false complete statuses can misstate business evidence. |
| Private bundle to sales projection | Expanded internal metrics must not enlarge public categories, fields or values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-04-01 | Tampering | Cost and cohort projections | mitigate | Stable canonical ordinals, cohort-key selection and exact partition/maturity counterexamples. |
| T-02-04-02 | Spoofing | Observed cash admissibility | mitigate | Preserve Phase 1 observed-source evidence and payable linkage controls; negative missing-evidence tests. |
| T-02-04-03 | Repudiation | Catalog and normalized outputs | mitigate | Exact source/cut/policy hashes, semantic paths, unique measure reconciliation identities and consumer-level equality tests. |
| T-02-04-04 | Information disclosure | Private/public publication | mitigate | Keep _private_root, public category/field/value allowlists and no-artifact failure tests unchanged. |
| T-02-04-05 | Denial of service | Canonical unit allocation | mitigate | Use integer prefix allocation arithmetic instead of expanding every sold unit; keep bounded numeric validation. |
| T-02-SC | Tampering | Package installation | accept | No installs or new dependencies; standard-library code and existing pinned environment only. |
</threat_model>

## Multi-source coverage audit

| Source | ID / item | Coverage | Status |
|---|---|---|---|
| GOAL | Exact coverage-aware costs, units, cash, learning, exceptions per cut | 02-01..03 baseline plus 02-04 tasks 1..3 | COVERED |
| REQ | MET-01 cost versions, allocations and economics | Task 1; task 3 normalized definitions/status | COVERED |
| REQ | MET-02 unit conservation | Existing 02-01 controls retained in task 1 and final regression; task 3 semantic coverage | COVERED |
| REQ | MET-03 obligations and layered cash | Task 3 canonical observed cash and both horizons | COVERED |
| REQ | MET-04 eligible product learning | Task 2 cohort projections; task 3 empty-domain cases | COVERED |
| REQ | MET-05 distinct budget states | Existing 02-02 conservation retained; task 3 individual state definitions and empty budget coverage | COVERED |
| REQ | MET-06 governed private/public exceptions | Existing 02-03 controls retained by tasks 2/3 and full regression | COVERED |
| REQ | MET-07 complete metric contracts | Task 3 semantic family catalog, registry, normalized output and failure controls | COVERED |
| RESEARCH | Final 22-source binding, immutable cut, policy authority, private atomic output | Existing binding retained; tasks 1..3 real build/bind tests and publication negatives | COVERED |
| RESEARCH | Integer SQL, Decimal ratios, residual cents, historical effective versions | Task 1 canonical allocation and task 3 faithful serializer | COVERED |
| RESEARCH | Coverage/zero/unknown/estimated, local half-open windows, 56/91 horizons | Tasks 1..3 exact counterexamples and empty-domain matrix | COVERED |
| RESEARCH | Cohort linkage/maturity, availability, lower-bound recorded unmet demand | Task 2 cohort controls and task 3 ZERO/MISSING cases; exposure regression retained | COVERED |
| RESEARCH | Native-grain cent/unit joins, supersession, independent balances, non-additive financial states | Task 3 cash evidence and existing finance/inventory gates | COVERED |
| RESEARCH | Privacy, policy/hash/path negatives, no new packages, full regression and verify_v2 | Task 3 final gates and threat model | COVERED |
| CONTEXT | D-01 canonical immutable cut | Tasks 1/3 actual intake and read-only binding | COVERED |
| CONTEXT | D-02 exact money and conservation | Tasks 1/3 exact oracles | COVERED |
| CONTEXT | D-03 coverage/time/lineage | All tasks | COVERED |
| CONTEXT | D-04 distinct canonical facts | Tasks 1/3 source-state controls | COVERED |
| CONTEXT | D-05 learning limitations | Tasks 2/3 cohort/exposure/demand safeguards | COVERED |
| CONTEXT | D-06 private analytical output | Tasks 2/3 public allowlists and private staging | COVERED |
| CONTEXT | D-07 compatibility and authority | Tasks 2/3 policy/status and historical regression | COVERED |

Deferred external owner/fiscal/real-data acceptance and Phase 3/4 interpretation/workbook/portal scope remain excluded, not missing source items. The verification report's non-blocking schema-runtime-link warning is not one of the five `gaps` and does not expand this repair.

<verification>
Record RED then GREEN for each named counterexample, using production projections and real intake/publication where specified. Preserve hand-computed expected cents, eligible units and Decimal strings; never set expected values by calling the implementation under test. Focused commands target under 60 seconds; measure actual durations. Full unittest and verify_v2 may exceed a minute and remain required. Run final gates after the last code change, preserving source/fixture bytes and recording interpreter, counts, exit codes, output path and relevant hashes in the summary. A failing intake, semantic mapping, privacy, policy, reconciliation or path negative must leave no published bundle. Independent phase re-verification remains required before the orchestrator marks Phase 2 complete.
</verification>

<success_criteria>
- Combined and partitioned 101-cent COGS agree; estimated dependent metrics remain ESTIMATED through JSON/CSV publication.
- The mixed cohort oracle yields eligible 1/10, with immature/unlinked evidence visible and UNKNOWN; foreign cohorts cannot alter quality rates.
- Valid observed 100-cent inflow passes Phase 1 and Phase 2; five layers are represented exactly at both horizons with truthful coverage and lineage.
- ZERO/MISSING unmet, sales, budget, payment and cash domains preserve unrelated publication without fabricated facts or false complete values.
- Every published decision measure has a complete definition and a matching normalized row at its declared grain; deleting both definition and normalized row cannot hide an orphan family measure.
- Existing stock, liability, budget, exception/privacy controls and final focused/full/verify_v2 gates pass.
</success_criteria>

<output>
Create `.planning/phases/02-reconciled-decision-metrics/02-04-SUMMARY.md` when implementation completes. Record the five gap outcomes, exact new test names, RED/GREEN evidence, final automated gates and any remaining verified limitations. Leave shared planning-state updates and phase re-verification to the orchestrator.
</output>
