---
phase: 04-offline-client-and-analyst-kit
plan: 06
type: tdd
wave: 6
depends_on: [04-05]
files_modified:
  - scripts/build_offline_portal_v1.py
  - scripts/package_client_v1.py
  - client/portal-v1.css
  - tests/test_offline_portal_v1.py
  - tests/test_weekly_kit.py
  - evidence/v1.0/portal/*
  - .planning/phases/04-offline-client-and-analyst-kit/04-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [CLIENT-01, CLIENT-02, CLIENT-03, CLIENT-04]
must_haves:
  truths:
    - "D-02: A verified current cut with 0/6, 1/6 or 5/6 accepted analyst roles shows Análisis pending, consistent with WAITING_ANALYSTS and the native-role panel; only 6/6 verified expected roles can complete that stage."
    - "D-03: Analysis completion does not imply independent reviewer acceptance, an owner decision or business execution; each remains in its separate verified state."
    - "D-02: Both public historical and safe synthetic-current file views have inspected evidence of actual browser zoom at 200 percent, including activation/readback, effective viewport and measured page overflow."
    - "D-02: Both views have an activated prefers-reduced-motion: reduce observation with matchMedia true and computed root scroll-behavior:auto."
    - "D-04: The committed visual receipt binds the observations and exact final HTML/CSS/capture hashes; the rebuilt audited public ZIP binds the new receipt SHA-256 and excludes private-current artifacts."
  artifacts:
    - path: scripts/build_offline_portal_v1.py
      provides: "Analysis-stage projection based on the complete verified expected-role set"
    - path: tests/test_offline_portal_v1.py
      provides: "Partial-role regression and measured zoom/motion receipt contracts"
    - path: scripts/package_client_v1.py
      provides: "Public-package receipt gate rejecting unsupported zoom/motion PASS claims"
    - path: evidence/v1.0/portal/portal-visual-inspection.json
      provides: "Inspected actual-zoom and reduced-motion observations bound to final rendered files"
    - path: .planning/phases/04-offline-client-and-analyst-kit/04-VERIFICATION.md
      provides: "Fresh independent phase truth assessment after gap closure"
  key_links:
    - from: scripts/build_offline_portal_v1.py
      to: alma/weekly_cycle.py
      via: "verify_cycle state expected_roles/accepted_roles, without changing native authority"
    - from: evidence/v1.0/portal/portal-visual-inspection.json
      to: scripts/package_client_v1.py
      via: "verified receipt SHA-256 in package manifest verification_inputs"
---

<objective>
Close the two actionable Phase 4 verification gaps: premature Análisis PASS with a strict subset of accepted native roles, and missing real-browser 200 percent zoom and reduced-motion proof. This preserves the offline workflow and audited public package required by CLIENT-01..04 and D-01..D-05.

Purpose: A client and analyst must see the true native state, while a reviewer can reproduce accessibility acceptance against the delivered bytes.
Output: Regression tests, corrected stage projection, measured browser evidence, a strengthened receipt/package gate, rebuilt public package and fresh phase verification.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-offline-client-and-analyst-kit/04-CONTEXT.md
@.planning/phases/04-offline-client-and-analyst-kit/04-UI-SPEC.md
@.planning/phases/04-offline-client-and-analyst-kit/04-VERIFICATION.md
@.planning/phases/04-offline-client-and-analyst-kit/04-05-SUMMARY.md
@scripts/build_offline_portal_v1.py
@scripts/package_client_v1.py
@tests/test_offline_portal_v1.py
@tests/test_weekly_kit.py
@evidence/v1.0/portal/portal-visual-inspection.json

<interfaces>
`collect_portal_model(mode="private", selected_cycle=cycle)` calls `verify_cycle(cycle)` and reads verified `state.json`; `state["expected_roles"]` maps six role IDs to request hashes and `state["accepted_roles"]` maps verified accepted responses. `_native_projection(cycle, state)` already marks missing roles `ESPERANDO_RESPUESTA`. The `stages` entry named `Análisis` currently uses `"PASS" if state.get("accepted_roles") else "ESPERANDO_RESPUESTA"` and causes the gap.

`tests.test_weekly_cycle.upstream`, `alma.weekly_cycle.start_cycle/record_dispatch/submit_response/verify_cycle`, and `tests.test_native_agents_v1.response_for/trace_for/receipt_for` create disposable valid role subsets; do not edit `state.json` directly because its hash/contract must remain valid. `scripts.package_client_v1._read_verified_receipt(path, "portal")` returns the receipt and SHA-256; `build_client_kit()` records that digest as `verification_inputs.portal_visual_inspection_sha256`, and `audit_client_zip()` checks it. The existing visual receipt has `surfaces.public` and `surfaces.synthetic_current`, each with `captures.zoom`, `rendered_portal.files`, `print`, and hashed `evidence_members`; its 720 CSS-pixel capture is a reflow equivalent rather than observed browser zoom.
</interfaces>
</context>

<source_audit>
| Source | Item | Coverage | Status |
| --- | --- | --- | --- |
| GOAL | Offline preparation, navigation, weekly run and package | 04-01..05 retain the workflow; 04-06 restores truthful status and acceptance evidence | COVERED |
| REQ | CLIENT-01 complete blank/synthetic workbook | 04-01 retained and affected package regression | COVERED |
| REQ | CLIENT-02 truthful offline portal and native roles | 04-02, 04-05, 04-06 tasks 1–3 | COVERED |
| REQ | CLIENT-03 supported weekly path | 04-03..04 retained; 04-06 focused weekly regression | COVERED |
| REQ | CLIENT-04 sanitized package | 04-03..05 retained; 04-06 receipt/ZIP rebind and audit | COVERED |
| RESEARCH | No Phase 4 RESEARCH.md; existing Phase 1–3 native and package contracts are fixed upstream | 04-06 uses current interfaces and adds no external dependency | COVERED |
| CONTEXT | D-01 workbook/source parity | 04-01 retained; package regression checks its receipt | COVERED |
| CONTEXT | D-02 current provenance, waiting, real-browser receipt | 04-06 tasks 1–3 | COVERED |
| CONTEXT | D-03 validated native authority and weekly path | 04-06 tests valid native submissions; weekly regression | COVERED |
| CONTEXT | D-04 public/private boundary and hash-bound ZIP | 04-06 task 3 | COVERED |
| CONTEXT | D-05 static local architecture | 04-06 changes existing Python/CSS/evidence only | COVERED |
| VERIFICATION | Análisis PASS at 1/6; real zoom and reduced motion unproven | 04-06 tasks 1–3 | COVERED |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing partial-role and visual-receipt contracts</name>
  <files>tests/test_offline_portal_v1.py, tests/test_weekly_kit.py</files>
  <behavior>
    - Valid synthetic cycles with 0/6, 1/6 and 5/6 accepted expected roles remain WAITING_ANALYSTS; `Análisis` and native panel remain pending. At 6/6 verified roles, `Análisis` may show PASS while reviewer and owner stay pending until their distinct evidence exists.
    - An accepted response outside the expected role set, an unverified response, or altered role evidence never counts toward analysis completion.
    - A receipt labeling a 720 CSS-pixel viewport `zoom_percent=200` without an actual browser zoom activation/readback and measured effective viewport fails; a receipt without activated and observed reduced motion in either surface fails, even if its capture hashes and top-level PASS are intact.
    - A valid receipt records both surface observations, exact final HTML/CSS and capture hashes, bounded inventory, and matching package verification input; stale receipt/ZIP combinations fail audit.
  </behavior>
  <action>Per D-02 and D-03, extend the existing disposable native-cycle fixture by submitting actual schema- and hash-validated analyst responses through `record_dispatch` and `submit_response`; parameterize 0, 1, 5 and 6 distinct expected roles in `test_analysis_stage_requires_all_verified_roles`. Assert verified cycle status, `Análisis`, native roles, reviewer and owner separately, including a rendered HTML check so the model and portal agree. In `PortalAccessibilityTests` and `ClientKitTests`, add negative controls in `test_visual_receipt_rejects_unmeasured_accessibility_claims`, derived from a deep copy of the committed receipt: missing zoom mechanism/readback, CSS-only viewport equivalence, missing effective-viewport/overflow measurements, missing matchMedia/computed-style observations, and one missing surface must fail. Define exact observation fields and require the package gate to reject these malformed PASS receipts. Run the focused tests and record RED against current production behavior; leave production/evidence bytes unchanged in this task.</action>
  <verify><automated>.venv\Scripts\python.exe -m unittest tests.test_offline_portal_v1.PortalEvidenceTests.test_analysis_stage_requires_all_verified_roles tests.test_weekly_kit.ClientKitTests.test_visual_receipt_rejects_unmeasured_accessibility_claims -v</automated></verify>
  <done>Focused tests reproducibly fail for 1/6 and 5/6 premature PASS and for the currently unsupported zoom/motion receipt, while valid zero-role behavior remains distinguished.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct verified analysis completion and receipt validation</name>
  <files>scripts/build_offline_portal_v1.py, scripts/package_client_v1.py, client/portal-v1.css</files>
  <behavior>
    - Only a complete verified set of expected analyst roles produces Análisis PASS; partial and waiting cycles cannot imply reviewer or owner approval.
    - Public packaging refuses a PASS visual receipt unless each safe surface records actual zoom activation/readback, effective viewport and overflow, and activated/observed reduced motion tied to exact rendered files.
  </behavior>
  <action>Per D-02/D-03, compute analysis completion from the verified `expected_roles` and `accepted_roles` keys after `verify_cycle`, requiring a nonempty exact expected set and every expected role accepted; preserve pending/partial language while any role is outstanding. Do not infer completion from response count alone or from the presence of a reviewer/packet; keep reviewer and owner stages derived from their existing evidence. Per D-02/D-04, tighten `_read_verified_receipt` for portal receipts so neither a CSS-only 720px reflow label nor unobserved motion can pass packaging; require each surface's same-session actual browser zoom method and readback at 200%, effective CSS viewport, document scroll/client widths with no page overflow, inspected PASS and a hashed zoom capture. Require activated `prefers-reduced-motion: reduce`, observed `matchMedia` true and computed root `scroll-behavior:auto`. Keep existing bounded inventory and public/private checks. If real-browser observation exposes a CSS defect, fix only the affected overflow/focus/motion rule in `client/portal-v1.css` and regenerate all evidence touched by changed CSS in Task 3. Follow RED→GREEN→REFACTOR, keeping Phase 1–3 calculations and native authority unchanged per D-05.</action>
  <verify><automated>.venv\Scripts\python.exe -m unittest tests.test_offline_portal_v1.PortalEvidenceTests.test_analysis_stage_requires_all_verified_roles tests.test_weekly_kit.ClientKitTests.test_visual_receipt_rejects_unmeasured_accessibility_claims -v</automated></verify>
  <done>The 0/1/5/6 role-state tests pass, malformed receipt negative controls fail closed, and only incomplete committed visual evidence still awaits recapture.</done>
</task>

<task type="auto">
  <name>Task 3: Capture actual accessibility behavior, rebind package and reverify Phase 4</name>
  <files>evidence/v1.0/portal/*, tests/test_offline_portal_v1.py, tests/test_weekly_kit.py, .planning/phases/04-offline-client-and-analyst-kit/04-VERIFICATION.md</files>
  <action>Per D-02/D-04, render exact final public historical and disposable safe synthetic-current file views; keep the current portal and source cut under ignored `.local/`. In a real Edge browser session, reset browser zoom to 100%, hold the same outer window size, activate the browser's own zoom control to 200% and read back its displayed/accessible zoom level. Capture a same-session screenshot for each context and record activation method, before/after browser zoom readback and CSS viewport/device-pixel-ratio measurements, `document.documentElement.scrollWidth` and `clientWidth`, and inspected navigation/provenance/warning disposition; a resized 720px viewport or device-scale emulation cannot substitute. In each same-context session activate `prefers-reduced-motion: reduce` through browser settings or CDP media emulation, record `matchMedia('(prefers-reduced-motion: reduce)').matches === true`, computed root `scroll-behavior === 'auto'`, and inspect focus/anchor behavior. Regenerate the zoom captures and any HTML/CSS-affected desktop/narrow/print captures; if CSS changes, rerender, rasterize and inspect every affected PDF page. Rehash every bounded evidence member, write a new `portal-visual-inspection.json` with both measured observations, exact rendered HTML/CSS hashes and per-surface inspected PASS, and run the negative receipt controls against it. Rebuild the public kit with `build_client_kit()` into a new ignored `.local/` path, run `audit_client_zip()`, verify its manifest's receipt SHA-256 equals the committed receipt, all allowlisted members and 22 extracted local links resolve, and no private HTML/captures, filled cuts or native responses enter. Run focused portal, weekly and package tests, `scripts/audit_release.py`, `git diff --check`, then independent Phase 4 re-verification against the 21 truths in `04-VERIFICATION.md`; record exact results and remaining uncertainty without claiming Phase 5 integrated acceptance. Preserve unrelated concurrent changes and do not rewrite earlier immutable run evidence.</action>
  <verify><automated>.venv\Scripts\python.exe -m unittest tests.test_offline_portal_v1 tests.test_weekly_kit -v</automated></verify>
  <done>Both contexts have inspected actual 200 percent browser zoom and reduced-motion PASS bound to final bytes; the new ZIP audits PASS with the new receipt hash and safe inventory; Phase 4 verification records the observed truth and remaining gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Control |
| --- | --- | --- |
| Verified native state → static portal | Partial response set appears terminal | Require complete expected-role set and independent reviewer/owner projections |
| Browser observation → PASS receipt | CSS reflow or unobserved motion masquerades as runtime acceptance | Browser zoom activation/readback and same-session geometry; matchMedia/computed-style observations |
| Private synthetic fixture → public ZIP | Current-cut or native material leaks | Ignored private fixture, strict allowlist, rebuilt ZIP audit and receipt hash binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-23 | Spoofing | Analysis stage | mitigate | Verify exact expected/accepted analyst role set before PASS; retain independent reviewer/owner states |
| T-04-24 | Tampering | Visual receipt | mitigate | Reject absent/contradictory zoom and motion observations; hash final HTML/CSS/captures and bound inventory |
| T-04-25 | Information disclosure | Public kit | mitigate | D-04 allowlist and postbuild archive/link audit; private current fixture remains under `.local/` |
| T-04-26 | Repudiation | Accessibility acceptance | mitigate | Record browser/version, activation/readback, same-session geometry, media result and inspected disposition per surface |
| T-04-27 | Elevation of privilege | Reviewer/owner stage | mitigate | D-03 separate reviewer and owner evidence; analysis completion cannot imply approval or execution |
| T-04-SC | Tampering | Package installs | accept | No new package installation or dependency is planned |
</threat_model>

<verification>
Confirm RED failures before code changes and GREEN after correction. Run `.venv\Scripts\python.exe -m unittest tests.test_offline_portal_v1 tests.test_weekly_kit -v`, affected weekly-cycle tests, `.venv\Scripts\python.exe scripts/audit_release.py` and `git diff --check` after final evidence bytes. Independently inspect the real-browser observations and rehash all receipt members, then run the Phase 4 verifier against all 21 truths. Treat the combined Phase 5 suite as a separate gate if concurrent Phase 5 files are still changing.
</verification>

<success_criteria>
The analysis-stage blocker and visual-evidence warning in `04-VERIFICATION.md` are closed by direct valid-cycle and real-browser evidence; CLIENT-02 has no unsupported PASS, and CLIENT-04's public package binds the corrected receipt without private material. The report states any remaining blocked or uncertain truth explicitly.
</success_criteria>

<output>
Create `.planning/phases/04-offline-client-and-analyst-kit/04-06-SUMMARY.md` after execution with RED/GREEN results, browser measurements, receipt/ZIP SHA-256 and Phase 4 re-verification outcome.
</output>
