---
phase: 04-offline-client-and-analyst-kit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_offline_portal_v1.py
  - client/portal-v1.css
  - tests/test_offline_portal_v1.py
autonomous: true
requirements: [CLIENT-02]
must_haves:
  truths:
    - "A client can open the complete static portal through file:// with no server and see a historical synthetic demo as the first public view (D-02)."
    - "A selected private cut shows its own verified provenance, status, metrics, exceptions, packet and lineage without mixing demo figures."
    - "Missing, stale, blocked or waiting native evidence is visibly labelled and never presented as owner-approved or measured zero."
  artifacts:
    - path: scripts/build_offline_portal_v1.py
      provides: Verified artifact loader and static public/private portal generator
    - path: client/portal-v1.css
      provides: Offline responsive, keyboard and print styles
    - path: tests/test_offline_portal_v1.py
      provides: Provenance, security, accessibility and link controls
  key_links:
    - from: scripts/build_offline_portal_v1.py
      to: alma/operating_workspace.py
      via: Phase 1 verify/manifest and cut identity
    - from: scripts/build_offline_portal_v1.py
      to: Phase 2 metric registry and report
      via: exact metric definitions and coverage state
    - from: scripts/build_offline_portal_v1.py
      to: Phase 3 task/packet/register artifacts
      via: verified hashes and actual dispatch/response state
---

<objective>
Generate the provenance-safe offline portal for CLIENT-02 and D-02 from verified historical and current-cut evidence.

Purpose: The owner can navigate sources, definitions, stages, native roles, decisions, exceptions and lineage without mistaking a synthetic demo or prepared task for current approved evidence.
Output: Static HTML builder, local CSS and focused tests; public and private portal instances remain physically separate.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/04-offline-client-and-analyst-kit/04-CONTEXT.md
@.planning/phases/04-offline-client-and-analyst-kit/04-UI-SPEC.md
@.planning/phases/03-governed-weekly-decision-cycle/03-RESEARCH.md
@scripts/build_client_system.py
@client/SISTEMA_ANALITICO.html
@agents/RUN-NATIVE-CYCLE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve verified portal evidence and dataset provenance</name>
  <files>scripts/build_offline_portal_v1.py, tests/test_offline_portal_v1.py</files>
  <behavior>Public generation selects labelled historical v0.2 synthetic evidence with no private cut; private generation accepts only a verified selected v1 cut and derives synthetic/privacy/current roles from its manifest; changed source/report/packet hashes make the view blocked.</behavior>
  <action>Per D-02 and D-05, read the final Phase 1 verify/manifest API, Phase 2 metric registry/report, and Phase 3 request, actual dispatch, packet and decision-register outputs before implementing the collector. Require an explicit selected private cut path under ignored `.local/` for private rendering, reverify source/report and available packet/register hashes at build, and form a stable view model containing cut ID, cutoff, timezone, full and short hashes, provenance flags, coverage/quality, stage receipts and status, sources with relationships, registered metrics, process/role contracts, four-part packets, decision events, exceptions and links. Keep `EJEMPLO SINTÉTICO · HISTÓRICO v0.2`, `CORTE PRIVADO ACTUAL · {cutoff} · {cut_id}`, `SIN CORTE PRIVADO SELECCIONADO`, and `CORTE BLOQUEADO · NO USAR PARA DECISIONES` labels exact where applicable. An actually synthetic selected cut carries both current and synthetic labels. A task request without a native receipt remains `ESPERANDO_RESPUESTA`; an advisory packet without owner registration remains advisory. Reject path escape, missing artifact and hash mismatch; never copy private evidence to public output.</action>
  <verify><automated>python -m unittest tests.test_offline_portal_v1.PortalEvidenceTests -v</automated></verify>
  <done>Public/demo, valid current, synthetic current, unselected, waiting and tampered fixtures produce the correct separate model/status; private paths cannot be passed to public generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the eight-section static portal with accessible local evidence links</name>
  <files>scripts/build_offline_portal_v1.py, client/portal-v1.css, tests/test_offline_portal_v1.py</files>
  <behavior>The generated HTML exposes all eight UI-spec sections in order and resolves every local link after staging; key content remains present with scripts disabled; badges, cut metadata and warnings remain visible in print and on data-bearing tables.</behavior>
  <action>Per D-02, render `Inicio / corte seleccionado`, `Recorrido semanal`, `Decisiones`, `Excepciones y calidad`, `Fuentes`, `Métricas`, `Procesos y roles`, and `Linaje y exportación` from the verified model. Follow the UI spec's Spanish copy, historical/current context actions, separate packet groups, exception-first ordering, searchable expandable source inventory, complete metric fields, actual dispatch identities/states, and source→mart→role→packet links. Use semantic header/nav/main/section, heading order, native details/summary, labelled search and result count, table captions/headers, visible focus and status words. Core content, navigation and print work without JS; any filter enhancement cannot hide evidence from no-script users. Reuse specified cream/forest/teal palette, system/Georgia typography, spacing and responsive breakpoints; keep asset paths local and relative. Include actual verified CSV/JSON/workbook/print links only when those artifacts exist, plus portable path/hash inventory. Escape all untrusted text/attributes; do not inject private absolute paths, remote URLs or client-supplied HTML. CSS print retains provenance, hashes, warnings and evidence names.</action>
  <verify><automated>python -m unittest tests.test_offline_portal_v1 -v</automated></verify>
  <done>Generated demo and private HTML contain all sections, required labels, provenance and verified relative links; disabled-script/print checks and hostile-text fixtures pass without cross-context figures or unsafe URLs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Private manifests/reports → HTML | Verified local artifacts contain values/text that must be escaped and restricted to ignored output. |
| Public synthetic evidence → ZIP | Only historical/generated synthetic facts and relative local assets may cross to client distribution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-04 | Spoofing | Current/demo provenance | mitigate | Derive cut role and synthetic flag from verified manifest, persist labels in headers, tables and print, and test mixed-context refusal. |
| T-04-05 | Tampering | Report/packet links | mitigate | Reverify exact artifact hashes before rendering and mark changed evidence blocked. |
| T-04-06 | Information disclosure | Private HTML/public output | mitigate | Enforce separate `.local/` private destination; reject private source in public build; render portable relative links only. |
| T-04-07 | Injection | HTML and href rendering | mitigate | Escape text/attributes, allowlist local relative links and reject external schemes and traversal. |
| T-04-SC | Tampering | Dependency install | accept | Use Python standard library and existing assets; no package install is required. |
</threat_model>

<verification>
Run focused tests, open generated public and private fixtures through `file://` in a browser, inspect at desktop/narrow widths and print preview, and confirm keyboard navigation/search/details and no-script content. The automated tests assert all local links resolve after copy to a temporary extracted directory.
</verification>

<success_criteria>
CLIENT-02 and D-02 hold when a verified selected cut and the historical synthetic demo never share measured values, every requested evidence section is navigable offline, and waiting/unknown/blocked states remain explicit.
</success_criteria>

<output>
Create `.planning/phases/04-offline-client-and-analyst-kit/04-02-SUMMARY.md` when done.
</output>
