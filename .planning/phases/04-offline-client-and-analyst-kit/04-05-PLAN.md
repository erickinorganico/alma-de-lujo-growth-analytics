---
phase: 04-offline-client-and-analyst-kit
plan: 05
type: tdd
wave: 5
depends_on: [04-04]
files_modified:
  - scripts/build_offline_portal_v1.py
  - client/portal-v1.css
  - tests/test_offline_portal_v1.py
  - tests/test_weekly_kit.py
  - evidence/v1.0/portal/*
autonomous: true
gap_closure: true
requirements: [CLIENT-02, CLIENT-04]
must_haves:
  truths:
    - "At 1440×900, the public and safe synthetic-current file:// portals show an unmistakable provenance strip, truthful status and summary cards, domain coverage with a paired table, and the next exception or decision before full hashes."
    - "The five display domains account for each of the 22 canonical source relations exactly once; historical sources use their own inventory and denominator, and source coverage states remain distinct from metric and process states."
    - "Observed zero, missing, partial, review, blocked and waiting remain distinct in the overview, chart, tables, CSV/JSON and print; public historical values never enter private-current summaries."
    - "The eight-section portal remains usable without JavaScript, by keyboard, with reduced motion, at 960, 390 and 320px and 200% zoom, and on every printed page."
    - "A new bounded visual PASS receipt binds the final HTML/CSS, viewport captures and every print/PDF page; rebuilding and auditing the public ZIP binds its exact new receipt SHA-256 without including private captures or current-cut HTML."
  artifacts:
    - path: scripts/build_offline_portal_v1.py
      provides: "Verified source-domain projection, truthful executive overview, semantic SVG/table parity and dense evidence sections"
    - path: client/portal-v1.css
      provides: "232px desktop rail, responsive reflow, accessible focus, reduced motion and complete print layout"
    - path: tests/test_offline_portal_v1.py
      provides: "Source-map, state, provenance, chart-table-export parity, semantic layout and bounded receipt assertions"
    - path: tests/test_weekly_kit.py
      provides: "Rebuilt ZIP receipt-hash, member-boundary and extracted-link assertions"
    - path: evidence/v1.0/portal/portal-visual-inspection.json
      provides: "New hash-bound public and safe synthetic-current browser/print inspection receipt"
  key_links:
    - from: scripts/build_offline_portal_v1.py
      to: alma/operating_contracts.py
      via: "SOURCE_NAMES and verified coverage; exact 22/22 presentation map, with no source silently dropped"
    - from: scripts/build_offline_portal_v1.py
      to: portal-model.json, metricas.csv, metricas.json
      via: "The same verified source counts and eligible metric rows feed inline SVG, semantic tables and exports"
    - from: evidence/v1.0/portal/portal-visual-inspection.json
      to: scripts/package_client_v1.py
      via: "_read_verified_receipt and package verification_inputs bind the new receipt hash"
---

<objective>
Close the visual BLOCK in 04-UI-REVIEW.md while preserving the verified Phase 4 analytical and package contracts, per CLIENT-02, CLIENT-04, D-02, D-04 and D-05.

Purpose: An owner can read the cut state, coverage and next action at a glance without mistaking a historical synthetic example, missing evidence or prepared native request for current approved results.
Output: A compact static executive portal, focused regression contracts, fresh browser/print evidence and a rebuilt audited public kit. No server, application framework, remote asset, new dependency or external business execution.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-offline-client-and-analyst-kit/04-CONTEXT.md
@.planning/phases/04-offline-client-and-analyst-kit/04-UI-SPEC.md
@.planning/phases/04-offline-client-and-analyst-kit/04-UI-REVIEW.md
@.planning/phases/04-offline-client-and-analyst-kit/04-VERIFICATION.md
@.planning/phases/04-offline-client-and-analyst-kit/04-02-SUMMARY.md
@.planning/phases/04-offline-client-and-analyst-kit/04-04-SUMMARY.md
@scripts/build_offline_portal_v1.py
@client/portal-v1.css
@tests/test_offline_portal_v1.py
@scripts/package_client_v1.py
@tests/test_weekly_kit.py
@alma/operating_contracts.py
</context>

<source_audit>
| Source | ID | Required outcome or constraint | Coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 4 | Prepare offline; navigate, run and package one local weekly workflow | 04-01..04 provide behavior; 04-05 closes executive usability and current package evidence | COVERED |
| REQ | CLIENT-01 | Complete blank/synthetic workbooks | 04-01, unchanged | COVERED |
| REQ | CLIENT-02 | Truthful offline portal, all eight sections and lineage | 04-02 plus this visual closure | COVERED |
| REQ | CLIENT-03 | Supported weekly creation/continuation with package stage | 04-03..04, unchanged | COVERED |
| REQ | CLIENT-04 | Audited sanitized offline kit | 04-03..04 plus rebuilt ZIP bound to this receipt | COVERED |
| RESEARCH | — | No Phase 4 RESEARCH.md exists; verified Phase 1–3 contracts remain upstream | Existing plans; no new external integration | COVERED |
| CONTEXT | D-01 | Registry-driven workbook and 22-relation parity | 04-01; this plan reads the same registry | COVERED |
| CONTEXT | D-02 | Public/private provenance, exact parity and hash-bound visual evidence | This plan replaces the visual receipt after redesign | COVERED |
| CONTEXT | D-03 | One supported launcher and truthful native waiting | 04-03..04, unchanged | COVERED |
| CONTEXT | D-04 | Public/private ZIP boundary and receipt binding | This plan rebuilds and audits the kit | COVERED |
| CONTEXT | D-05 | Local Python/SQLite/CSV/JSON/XLSX/static HTML architecture | This plan adds only generated HTML/CSS and existing tests | COVERED |
| UI REVIEW | BLOCK + warnings | Compact overview, domain chart/table, navigation, dense evidence, responsive/zoom, contrast, print and new receipt | Tasks 1–3 | COVERED |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish failing executive-portal and package contracts</name>
  <files>tests/test_offline_portal_v1.py, tests/test_weekly_kit.py</files>
  <behavior>
    - A canonical source-domain map partitions `SOURCE_NAMES` exactly once into Producto y ventas (sku_catalog, sales_aggregates, availability_daily, unmet_demand, sales_readiness), Inventario y calidad (inventory_counts, inventory_movements, inventory_reservations, quality_events, loans), Costos y compras (cost_versions, cost_components, cost_allocations, purchase_orders, purchase_receipts), Caja y obligaciones (obligations, obligation_payments, cash_events, cash_balance_evidence), and Presupuesto y gastos (budgets, budget_allocations, expenses); totals are 5+5+5+4+3=22, and an unmapped/duplicate v1 ID fails rendering.
    - V1 coverage SVG segments and paired semantic table reconcile integer counts and denominators for COMPLETE, ZERO, PARTIAL, ESTIMATED, MISSING, ERROR and NOT_APPLICABLE without merging their meanings. A historical fixture counts only its actual sources and says Inventario histórico sintético; no-cut and unverified sets never show numeric zero as a measured count.
    - Public historical, unselected private, selected private synthetic, selected private non-synthetic and invalid/hash-mismatched states have the exact distinct labels and warning/empty behavior from 04-UI-SPEC. `WAITING_ANALYSTS` remains pending; zero exceptions or decisions is shown only for a verified empty set. Full hashes remain selectable in Detalles del corte y procedencia and Linaje, after the overview.
    - Any optional metric bars require at least two verified same-cut values with equal unit, population/grain and window and permitted comparison; their values/status/source hashes match the metric table and CSV/JSON. Ineligible rows show the exact No hay una serie comparable... copy. No line/trend appears without three independently verified comparable dated observations.
    - Generated HTML retains eight anchors, semantic SVG title/description plus paired table, labelled focusable table scroll regions, skip link, ordered headings, no-JavaScript critical content and local-only links. CSS contracts cover 232px desktop rail, four-size/two-weight type scale, 320px and 200% reflow, reduced motion, visible contrast-safe focus and print-preserved evidence.
    - The bounded visual receipt asserts exact final public/private-synthetic HTML/CSS hashes, 1440×900/960/390/320/200%-zoom captures, every print/PDF page and per-surface PASS; package manifest binds its new SHA-256 and excludes private HTML/captures.
  </behavior>
  <action>Extend the existing synthetic fixtures and independent mart→portal→CSV→JSON oracle before production edits. Add narrow `PortalExecutiveContractTests` using DOM/SVG parsing and direct source-count arithmetic, including adversarial unknown source, missing/zero, mixed-unit/window, stale hash, escaped text and no-JavaScript cases. Assert structure and truthful labels, not a screenshot's pixel positions. Replace hard-coded legacy receipt page/member counts in `PortalAccessibilityTests` with exact bounded inventory and all-page hash checks derived from the new receipt schema. Extend `ClientKitTests` to compare the package manifest's `portal_visual_inspection_sha256` to the new receipt bytes, verify extracted local links, and reject private/current artifacts. Record RED failures attributable to the missing executive layout and new receipt. Preserve unrelated tests and the Phase 5 deep content-mutation audit boundary.</action>
  <verify><automated>.venv\Scripts\python.exe -m unittest tests.test_offline_portal_v1.PortalExecutiveContractTests -v</automated></verify>
  <done>Focused contracts fail for the current hero/chart/source-map/layout and stale receipt, with a recorded RED result and no production edit yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the truthful offline executive portal</name>
  <files>scripts/build_offline_portal_v1.py, client/portal-v1.css</files>
  <behavior>
    - The new executive, provenance, source-map and chart-parity contracts pass without changing Phase 1–3 calculations or the exact exported metric values.
    - Public historical and private current views keep separate role and synthetic attributes; unselected/blocked views cannot inherit another context's figures.
    - All 22 v1 relations and every source coverage state remain visible in chart/table/source inventory, while decisions and exceptions stay actionable and traceable.
  </behavior>
  <action>In `scripts/build_offline_portal_v1.py`, derive the five display-domain groups from the exact 22 IDs above and assert equality with `SOURCE_NAMES` before rendering a v1 chart. Use the already verified model's source status and report coverage only; count COMPLETE and ZERO in separate verified segments, PARTIAL and ESTIMATED separately, MISSING as unknown, ERROR as blocked, and NOT_APPLICABLE explicitly in the denominator. For historical mode, group only present historical sources with their actual statuses and a historical denominator. Emit zero-based inline SVG with title/description, direct labels and a same-section semantic table showing domain, verified, partial, unknown/blocked and total; test chart→table arithmetic independently. Gate the optional `Métricas comparables` chart on same cut, explicit unit, population/grain, window and permitted metric definition; preserve signed values around zero and exact table/export parity. If the gate fails, render the contract's exact no-series sentence, never a trend or fabricated delta.

Restructure `_render_html()` and `client/portal-v1.css` into the specified 232px sticky desktop rail and compact main work area: eight existing anchor destinations in two groups, header ≤88px at 1440, 48–64px provenance strip, dominant state card plus source/exception/decision cards, 2:1 coverage/next-step row, then the highest-priority operating table. Show truthful Spanish status, next unmet gate and contextual local CTA; keep full cut ID/hashes in a reachable `Detalles del corte y procedencia` disclosure and Linaje. Place exceptions before PASS, retain seven stage states, dense decision/packet/metric/source tables, exact four packet groups, numeric alignment and focusable labelled overflow regions. Preserve the public historical, selected private synthetic and unselected copy from the UI specification; when selected-cut verification fails, emit only the specified blocked diagnostic state with the failed evidence and local correction path, never stale metrics or a normal dashboard. Escape every value and avoid private absolute paths in the diagnostic. Use the specified 4/8/16/24/32/48 spacing rhythm, 14/16/20/28px type and 400/600 weights, forest/cream/teal palette with teal reserved for primary action/active nav/evidence links, WCAG AA text and ≥3:1 focus/boundary contrast. Reflow to horizontal anchor strip below 1200, one-column modules below 600, 16px mobile gutters, no fixed body width or page-level overflow; retain complete static content when script is disabled. Ensure reduced-motion behavior and grayscale print with repeated provenance/status header, full hashes, SVG text/table and uncut rows. Do not add remote assets, network calls, new dependencies, server functions, false owner actions or altered data semantics.</action>
  <verify><automated>.venv\Scripts\python.exe -m unittest tests.test_offline_portal_v1.PortalExecutiveContractTests tests.test_offline_portal_v1.PortalEvidenceTests -v</automated></verify>
  <done>Focused RED contracts turn GREEN; both public and private synthetic portals render a consistent executive overview with exact source/chart/table parity and retained evidence boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Reinspect exact final bytes and rebuild the audited public kit</name>
  <files>evidence/v1.0/portal/*, tests/test_offline_portal_v1.py, tests/test_weekly_kit.py</files>
  <action>Generate fresh public historical and safe synthetic-current portals from verified fixtures after the final HTML/CSS edit; keep current-cut output and captures under ignored `.local/` until only bounded synthetic visual evidence is deliberately recorded. Open the exact `file://` outputs in the available real browser and capture each context at 1440×900, 960px, 390px, 320px and 200% zoom. Inspect first-screen order, 320/zoom document scroll width, all eight anchors, Tab/Enter and horizontal table keyboard scroll, no-JavaScript content, reduced motion, local extracted links, public/private labels, source SVG/table parity and contrast. Print both contexts to PDF, rasterize and inspect every page for repeated provenance/cutoff/status, full hash appendix, warnings, table headers and no clipped rows/graphs; keep grayscale legibility. Replace superseded evidence only after safely inventorying the known generated `evidence/v1.0/portal/` members. Write a fresh `portal-visual-inspection.json` with browser/version, timestamp, viewport/zoom, inspected page counts, per-surface PASS/BLOCKED, exact final HTML/CSS and screenshot/PDF/page SHA-256, no absolute/private paths, and an exact bounded member set; do not reuse the former PASS or hard-coded 11/48 page counts. Rebuild the public ZIP through `build_client_kit()` and `audit_client_zip()`, rehash its manifest `portal_visual_inspection_sha256` against the new receipt, inspect every allowlisted member and extracted link, and prove private current HTML/captures, filled cuts, native responses and credentials are absent. Check that the existing weekly package stage still binds the new deterministic ZIP without changing `WAITING_ANALYSTS` or earlier immutable run bytes. Run focused portal/package tests, privacy audit and affected full regressions. Record the exact new evidence and audit results in `04-05-SUMMARY.md`; do not claim owner adoption, fiscal approval or observed business impact.</action>
  <verify><automated>.venv\Scripts\python.exe -m unittest tests.test_offline_portal_v1.PortalAccessibilityTests tests.test_weekly_kit.ClientKitTests -v</automated></verify>
  <done>Every required screen/zoom and printed page has inspected PASS tied to final hashes; the rebuilt ZIP audits PASS, binds the new receipt, extracts with working local links and contains only allowed public material.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Control |
| --- | --- | --- |
| Verified manifest/report → generated HTML/SVG | Stale or hostile values become apparently current evidence | Revalidate cut hashes; escape text/attributes; derive counts from verified model; fail unknown source IDs |
| Private current cut → public package | Private figures or captures enter synthetic distribution | Physically separate `.local/` current portal; strict ZIP allowlist, receipt binding and extracted-member audit |
| Browser evidence → PASS receipt/package | An old screenshot or incomplete print review authorizes changed bytes | New exact HTML/CSS/image/PDF hashes, viewport and all-page inventory; fail absent or changed members |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-17 | Spoofing | Portal provenance | mitigate | D-02 role, synthetic flag, cutoff and status derive from verified manifest; tests reject cross-context values and stale hashes |
| T-04-18 | Tampering | Coverage SVG/table | mitigate | Exact 22/22 map and independent segment/table arithmetic; fail unmapped/duplicate IDs and altered model inputs |
| T-04-19 | Information disclosure | Public ZIP | mitigate | D-04 strict member allowlist, receipt-hash and extracted-content checks; private portal and browser captures remain outside ZIP |
| T-04-20 | Repudiation | Visual PASS receipt | mitigate | Record browser/version, every required viewport/zoom, all print pages and SHA-256 bound to final HTML/CSS and package manifest |
| T-04-21 | Elevation of privilege | Decision/action copy | mitigate | Static local links only; advisory packet, native waiting and owner registration remain separate states per D-03/D-05 |
| T-04-22 | Supply chain | Dependencies | accept | No install or new dependency is part of this plan; use existing pinned local Python/browser tools |
</threat_model>

<verification>
Run `.venv\Scripts\python.exe -m unittest tests.test_offline_portal_v1 tests.test_weekly_kit -v`, then `.venv\Scripts\python.exe -m unittest discover -s tests -v` and `.venv\Scripts\python.exe scripts/audit_release.py` after final evidence and package bytes exist. Recheck `git diff --check` and the generated ZIP member/hash/link/privacy inventory. Visual acceptance is blocking: each of the two safe contexts must have PASS at 1440×900, 960, 390, 320 and 200% zoom, keyboard/no-JavaScript/reduced-motion checks, plus every PDF page inspected from exact final bytes. Re-run focused tests if any production/evidence bytes change afterward.
</verification>

<success_criteria>
CLIENT-02's executive visual contract passes 04-UI-REVIEW's three priority fixes and all source/status/parity boundaries; CLIENT-04's rebuilt package binds the new PASS visual receipt and excludes private/current material. The prior 04-VERIFICATION.md package-stage gap remains closed by dependency 04-04; this plan does not assert closure of the deferred Phase 5 deep content-mutation audit.
</success_criteria>

<output>
Create `.planning/phases/04-offline-client-and-analyst-kit/04-05-SUMMARY.md` when done.
</output>
