---
phase: 04-offline-client-and-analyst-kit
plan: 04
type: tdd
wave: 4
depends_on: [04-03]
files_modified:
  - alma/weekly.py
  - tests/test_weekly_kit.py
  - docs/ANALYST-WEEKLY-v1.md
autonomous: true
gap_closure: true
requirements: [CLIENT-03]

must_haves:
  truths:
    - "The supported run.ps1 weekly command builds the sanitized public client ZIP before returning WAITING_ANALYSTS and exposes a bounded package-stage result without claiming native analysis, review, owner approval, or external execution."
    - "The immutable run records a canonical package receipt and run-index entry containing a run-relative package path, ZIP SHA-256, package-manifest SHA-256, and PASS audit dispositions for allowlist/privacy/links."
    - "Identical public inputs in separate new runs produce the same package hash; replay against an existing cut refuses mutation, and a package build or audit failure removes the entire newly prepared cut."
    - "A fresh process from a clean committed checkout revalidates the package bytes, receipt and run index before any weekly-resume action, while tampered package evidence fails closed and earlier bytes remain unchanged."
  artifacts:
    - path: "alma/weekly.py"
      provides: "Integrated public-kit stage, safe receipt projection, immutable index binding and restart verification"
      contains: "build_client_kit"
    - path: "tests/test_weekly_kit.py"
      provides: "Executable package-stage, idempotency, atomic-failure, tamper and clean-restart contracts"
      contains: "WeeklyPackageStageTests"
    - path: "docs/ANALYST-WEEKLY-v1.md"
      provides: "One supported command path and exact package receipt/readback instructions"
      contains: "public-package.json"
  key_links:
    - from: "alma/weekly.py"
      to: "scripts/package_client_v1.py"
      via: "build_client_kit(output) followed by audit_client_zip(output), with no private cut or native artifact supplied to either function"
      pattern: "build_client_kit|audit_client_zip"
    - from: "alma/weekly.py"
      to: "receipts/public-package.json"
      via: "canonical immutable stage receipt whose SHA-256 is bound in run-index.json"
      pattern: "public-package\\.json"
    - from: "run.ps1"
      to: "alma.weekly create_weekly_run"
      via: "the existing weekly route; no detached package command is required"
      pattern: "alma\\.weekly"
---

<objective>
Close the single Phase 4 CLIENT-03 blocker by making public-kit creation and immutable verification part of the supported weekly command itself, per D-03, D-04 and D-05.

Purpose: The analyst must receive one truthful, restart-safe command path from source intake through a receipted sanitized package. The package remains public blank/synthetic material and is independent of private analysis status.
Output: Failing contract tests followed by an integrated package stage, exact immutable evidence, clean-restart validation and corrected analyst instructions.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-offline-client-and-analyst-kit/04-CONTEXT.md
@.planning/phases/04-offline-client-and-analyst-kit/04-VALIDATION.md
@.planning/phases/04-offline-client-and-analyst-kit/04-VERIFICATION.md
@.planning/phases/04-offline-client-and-analyst-kit/04-03-SUMMARY.md
@alma/weekly.py
@scripts/package_client_v1.py
@tests/test_weekly_kit.py
@docs/ANALYST-WEEKLY-v1.md
</context>

<source_audit>
| Source | ID | Feature or constraint | Coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 4 | Client prepares a cut offline and analyst navigates, runs and packages the local workflow | 04-01..04, with the disconnected package link closed here | COVERED |
| REQ | CLIENT-01 | Complete validated workbook inputs | Existing 04-01 | COVERED |
| REQ | CLIENT-02 | Provenance-safe offline portal | Existing 04-02 | COVERED |
| REQ | CLIENT-03 | One documented no-server workflow through package | 04-03 plus this gap closure | COVERED |
| REQ | CLIENT-04 | Sanitized offline package | Existing 04-03; this plan only invokes and binds it | COVERED |
| RESEARCH | none | No Phase 4 RESEARCH.md exists; verification supplies the only actionable gap | 04-VERIFICATION.md gap | COVERED |
| CONTEXT | D-01 | Registry-complete workbook and canonical pack | Existing 04-01 | COVERED |
| CONTEXT | D-02 | Truthful offline portal | Existing 04-02 | COVERED |
| CONTEXT | D-03 | Supported weekly launcher creates and receipts the public kit | This plan | COVERED |
| CONTEXT | D-04 | Public/private distribution boundary | Existing 04-03 plus invocation-only wiring here | COVERED |
| CONTEXT | D-05 | Local existing architecture with no new provider or server | This plan reuses existing Python functions and dependencies | COVERED |

The deferred self-consistent secret/PII mutation audit is excluded from this gap plan and remains owned by Phase 5 Plan 05-03. This plan does not change `audit_client_zip()` or claim that deeper content scan.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing integrated package-stage contracts</name>
  <files>tests/test_weekly_kit.py</files>
  <behavior>
    - A successful `create_weekly_run()` produces one run-relative public ZIP, `receipts/public-package.json`, a matching `run-index.json` package binding, and a bounded CLI result with exact package path/hash/manifest/audit fields while the run remains `WAITING_ANALYSTS` and `native_execution_claimed=false`.
    - The receipt and index hashes equal direct reads; `audit_client_zip()` returns PASS; the ZIP contains no private run, source workbook values, native request/response/trace, register event, absolute path, or `.local` member.
    - Two distinct new output roots built from identical public inputs yield identical ZIP and manifest hashes. A repeated call for the same cut fails without changing any existing run byte.
    - If `build_client_kit()` or the explicit post-build `audit_client_zip()` check raises after creating a partial target, the new cut directory and partial public artifact are absent afterward.
    - A second process launched from a clean `git archive` resumes an explicitly restored private run only after package bytes and receipt/index bindings revalidate; package or receipt tampering returns the weekly verification error and no earlier receipt is rewritten.
    - The analyst guide states that `run.ps1 weekly` creates the public package automatically and identifies its run-relative ZIP, receipt and audit fields; it no longer instructs the analyst to execute the Python packager as a detached required stage.
  </behavior>
  <action>Create `WeeklyPackageStageTests` before production edits, using disposable synthetic fixtures and `unittest.mock` only. Assert exact key sets for the package projection, stage receipt and run-index binding so private/native data cannot silently enter them. Reuse the existing clean-archive subprocess pattern and direct SHA-256 reads. Exercise both builder failure and post-build auditor failure, preserve a byte inventory for the existing-run replay and restart assertions, and inspect the package member inventory rather than assuming sanitation from a filename. Add the exact analyst-guide assertions to `GuideContractTests`. Run the focused class and guide test and record the intended RED failures caused by the missing package stage and detached guide. Do not weaken existing Phase 4 tests. Do not implement or broaden the deeper content/secret scanner assigned to 05-03.</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m unittest tests.test_weekly_kit.WeeklyPackageStageTests tests.test_weekly_kit.GuideContractTests -v (expected non-zero RED caused only by the absent integrated package contract)</automated>
  </verify>
  <done>The committed tests fail for the missing package ZIP/receipt/index/result/restart behavior and detached guide, while existing unrelated weekly behavior remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate, receipt and revalidate the sanitized public package</name>
  <files>alma/weekly.py, docs/ANALYST-WEEKLY-v1.md</files>
  <behavior>
    - The new RED contracts pass without changing `scripts/package_client_v1.py` or the truthful native/owner state machine.
    - Package failure is atomic with the rest of new-run preparation; success is immutable and restart-verifiable.
  </behavior>
  <action>In `alma/weekly.py`, import and reuse `scripts.package_client_v1.build_client_kit` and `audit_client_zip`; do not reproduce ZIP logic. After the Phase 3 cycle has reached honest `WAITING_ANALYSTS` and before publishing the run index, call the builder with only a new path such as `public-kit/Alma_OS_Client_v1.zip`. Never pass the private run, filled workbook, canonical private pack, current report, cycle, native evidence or register to the builder. Explicitly call `audit_client_zip()` on the completed bytes and require the builder and auditor to agree on PASS, ZIP SHA-256, manifest SHA-256, member count, privacy and links. Write one canonical immutable `receipts/public-package.json` with an exact allowlist containing version/status, run-relative package path, ZIP SHA-256, manifest SHA-256, member count and audit dispositions; bind its SHA-256 in `run-index.json`, add the same safe package identity under a single `public_package` index object, and expose that object in the safe command result. Keep `status=WAITING_ANALYSTS`, `external_execution=PROHIBITED`, and all native/owner claims unchanged. Extend `_load_run()` to reject path escape/linking, receipt/index disagreement, missing or altered ZIP bytes and a failed fresh `audit_client_zip()` before any continuation action. Rely on the existing whole-destination cleanup for builder/auditor/receipt failure, ensuring any partial package is also removed. Preserve immutable existing-cut refusal and deterministic package bytes across different output roots. Update `docs/ANALYST-WEEKLY-v1.md` so the exact existing `run.ps1 weekly` command documents automatic package output, the safe JSON/result fields, the immutable receipt/index binding and restart verification; retain record/submit/resume/packet/register and real native dispatch requirements. State that the package is public blank/synthetic material independent of `WAITING`, `BLOCKED`, `REVIEW` or `READY_FOR_OWNER`, and that it never proves approval or external execution. Do not add a new CLI action, server, dependency or package-content scanner.</action>
  <verify>
    <automated>.venv/Scripts/python.exe -m unittest tests.test_weekly_kit.WeeklyPackageStageTests tests.test_weekly_kit.WeeklyCommandTests tests.test_weekly_kit.ClientKitTests tests.test_weekly_kit.GuideContractTests -v &amp;&amp; .venv/Scripts/python.exe -m unittest tests.test_client_system tests.test_client_review tests.test_security -v &amp;&amp; .venv/Scripts/python.exe -m unittest discover -s tests -v &amp;&amp; .venv/Scripts/python.exe scripts/audit_release.py</automated>
  </verify>
  <done>`run.ps1 weekly` creates and reports a deterministic sanitized package as an immutable stage, any failed/tampered/replayed attempt fails atomically or without mutation, a clean process can verify and continue the run, every native state remains honest, the guide describes the one supported path, and all Phase 4/package/privacy/full regressions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Private weekly run -> public kit builder | Filled source, marts, cycle, native evidence and decisions must never become builder inputs or ZIP members. |
| Package bytes -> receipt/index/result | Hashes, paths and audit dispositions must describe the exact immutable ZIP without leaking private values or absolute paths. |
| Restored private run -> weekly continuation | A new process must authenticate all earlier receipt, index and package bindings before continuing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-12 | Information disclosure | `create_weekly_run` package invocation | mitigate | Call only the existing public blank/synthetic builder with its output path and inspect the exact ZIP member set; never pass private cut artifacts. |
| T-04-13 | Tampering | package receipt, run index and ZIP | mitigate | Canonical exact-key receipt, direct SHA-256/manifest binding, immutable index hash and fresh restart audit fail closed on drift. |
| T-04-14 | Repudiation | weekly command result | mitigate | Return the safe package identity and audit disposition while retaining WAITING/PROHIBITED authority states. |
| T-04-15 | Denial of service | partial package generation | mitigate | Build inside the new run's atomic preparation boundary and delete the entire new destination on builder, auditor or receipt failure. |
| T-04-16 | Elevation of privilege | analysis/owner state | mitigate | Package success cannot advance native analysts, reviewer, owner decision or external execution state. |
| T-04-SC | Tampering | dependency install | accept | No package install or dependency change occurs; the plan reuses already audited repository modules. |
</threat_model>

<verification>
Execute RED before GREEN, then run the focused weekly/package/guide classes, package/privacy regressions, full repository suite and publishable-tree audit. Re-run the clean `git archive` second-process continuation with the ignored private run supplied explicitly; compare the initial index, receipt and package bytes before and after. Confirm the only deferred finding remains the Phase 5 05-03 deep content scan and make no PASS claim for it here.
</verification>

<success_criteria>
CLIENT-03 and the Phase 4 goal pass when the existing supported weekly command produces the public kit as a verified immutable stage, exposes its safe path and hashes, leaves native/owner states honest, survives a clean-process restart, and fails atomically or without mutation on builder, auditor, tamper or replay errors. No separate packaging command is required for the documented workflow.
</success_criteria>

<output>
Create `.planning/phases/04-offline-client-and-analyst-kit/04-04-SUMMARY.md` when done.
</output>
