---
phase: 04-offline-client-and-analyst-kit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_operating_workbooks.py
  - alma/operating_workbook.py
  - client/v1/policies/operating-metrics-review-template-v1.json
  - client/v1/policies/operating-metrics-synthetic-v1.json
  - tests/test_operating_workbooks.py
  - evidence/v1.0/workbooks/workbook-pack-parity.json
autonomous: true
requirements: [CLIENT-01]
must_haves:
  truths:
    - "A client can open a blank or visibly synthetic v1 workbook and find every source relation in the versioned registry exactly once (D-01)."
    - "An analyst can export edited workbook rows to a source pack accepted by the Phase 1 intake contract, with unknown distinct from measured zero."
    - "An independent workbook→canonical-pack→manifest oracle proves exact relation, row, null/zero, integer-cent, unit and source-hash parity for all 22 relations."
    - "The client kit contains a versioned unapproved REVIEW policy template and a verified synthetic policy example, separate from the 22 Phase 1 source relations and with no false owner approval."
    - "A workbook error names the sheet, row, field, issue and next correction without changing the submitted workbook."
  artifacts:
    - path: scripts/build_operating_workbooks.py
      provides: Registry-driven blank/example XLSX and matching source-pack generation
    - path: alma/operating_workbook.py
      provides: Exact-sheet workbook read, diagnostics and CSV export adapter
    - path: client/v1/policies/operating-metrics-review-template-v1.json
      provides: Versioned unapproved management-policy template conforming to the Phase 2 schema
    - path: client/v1/policies/operating-metrics-synthetic-v1.json
      provides: Verified synthetic management-policy example with no real-cut authority
    - path: tests/test_operating_workbooks.py
      provides: Relation coverage, round-trip and adversarial workbook assertions
    - path: evidence/v1.0/workbooks/workbook-pack-parity.json
      provides: Safe synthetic, hash-bound workbook→pack→manifest oracle receipt covering all 22 relations
  key_links:
    - from: scripts/build_operating_workbooks.py
      to: alma/operating_contracts.py
      via: final Phase 1 ordered relation registry
    - from: alma/operating_workbook.py
      to: alma/operating_interchange.py
      via: final Phase 1 source-pack validation
    - from: tests/test_operating_workbooks.py
      to: generated workbook, canonical CSV pack and workbook manifest
      via: independent workbook→pack→manifest parity oracle over all 22 relations
---

<objective>
Deliver contract-complete blank and synthetic example operational workbooks, separate REVIEW/synthetic policy materials, and an exact workbook-to-v1-source-pack adapter for CLIENT-01 and D-01.

Purpose: The client can enter a weekly cut in a readable offline spreadsheet while the analyst imports only source data that satisfies the canonical CSV contract.
Output: Builder, adapter, versioned policy materials and focused tests; generated artifacts are emitted to caller-provided paths and later packaged by 04-03.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-offline-client-and-analyst-kit/04-CONTEXT.md
@.planning/phases/04-offline-client-and-analyst-kit/04-UI-SPEC.md
@.planning/phases/01-versioned-operating-intake/01-RESEARCH.md
@scripts/build_client_workbook.py
@client/contract.json
@alma/client_review.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate every v1 input relation as an accessible blank and synthetic workbook</name>
  <files>scripts/build_operating_workbooks.py, client/v1/policies/operating-metrics-review-template-v1.json, client/v1/policies/operating-metrics-synthetic-v1.json, tests/test_operating_workbooks.py</files>
  <behavior>Enumerating the final Phase 1 source registry yields exactly one table sheet per relation in each workbook, in declared domain and weekly-entry order; blank sheets have no data rows; example sheets contain one registry-valid invented synthetic row per relation or a documented interrelated synthetic fixture when FK validation requires it. Headers and cell values round-trip without loss. A client-facing completeness matrix identifies required and optional relations and the exact dependent metric state (`UNKNOWN`, `PARTIAL`, `REVIEW` or `BLOCKED`) when evidence is omitted. A parameterized omission suite evaluates every one of the 22 relations independently, plus declared interacting omissions, so no relation-to-metric mapping can pass only because a combined minimal fixture masks it. The REVIEW policy template and synthetic policy validate against the Phase 2 schema, are hash-manifested separately, and are never counted as source relations or owner approval.</behavior>
  <action>Use same-task TDD: first create or extend `OperatingWorkbookGenerationTests` with registry completeness, policy-status, completeness-matrix and manifest assertions. Parameterize the suite over all 22 registry relations: for each relation assert required/optional classification, the affected metric families and the exact `UNKNOWN`, `PARTIAL`, `REVIEW` or `BLOCKED` state when that relation alone is omitted; add combined omission cases where metric dependencies interact. Run the focused command and record its expected RED result, then implement until GREEN. Per D-01 and D-05, inspect the completed Phase 1 registry, source-pack initializer and summary; consume their final relation IDs, ordered headers, types, nullability, enums, keys, units and provenance rather than maintaining a second sheet list. Also inspect the completed Phase 2 `contracts/operating-metrics-policy-v1.schema.json`, metric registry and `policies/operating-metrics-synthetic-v1.json`. Build paired XLSX and matching blank/example CSV packs to a caller-selected output directory, with `INICIO`, glossary/dictionary, contract version, cutoff/timezone, per-domain synthetic labels, source lineage, required/optional relation matrix, each affected metric family and its exact omission state, and file/hash manifest. Create a separate versioned policy template with status `REVIEW` and no owner approval reference, and copy/validate the canonical synthetic example as `SYNTHETIC_EXAMPLE`; include both in the generated manifest without adding either to the 22-relation count. Match existing XlsxWriter/openpyxl patterns and UI specification: blue editable inputs with legend, protected computed cells if present, controlled dropdown/date/number/duplicate-key validation, frozen titles and headers, filters, widths, landscape print settings and repeated headers. Keep canonical headers unchanged and no macros, network links, credentials, or private records. Generated examples must pass the Phase 1 pack validator, including FK relationships, while both policy files must pass the Phase 2 schema/hash/status validator.</action>
  <verify><automated>.venv\Scripts\python.exe -m unittest tests.test_operating_workbooks.OperatingWorkbookGenerationTests -v</automated></verify>
  <done>Generated blank/example packs enumerate every final registry relation exactly once, parse with correct version and ordered headers, and the example passes upstream whole-pack validation; the workbook and guide expose one authoritative requiredness/completeness matrix and the 22-relation parameterized omission suite proves each stated metric state; separate REVIEW/synthetic policies validate, contain no false approval and appear in the manifest; the test checks visual/print metadata and synthetic markers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate and export workbook input through the canonical v1 intake contract</name>
  <files>alma/operating_workbook.py, tests/test_operating_workbooks.py, evidence/v1.0/workbooks/workbook-pack-parity.json</files>
  <behavior>Valid workbook rows become exact ordered CSV source rows and manifest data; blank nullable cells stay null while explicit zero stays zero; exact integer cents, units, ordered fields and source hashes agree across workbook, canonical pack and manifest. Extra/repeated/missing sheet or column, bad type/date/unit, duplicate key, broken relation, formula in an input, manifest divergence or PII-like field rejects before a cut is published.</behavior>
  <action>Use same-task TDD: first create or extend `WorkbookImportTests` for the explicit workbook→canonical-pack→manifest oracle, exact export, null/zero, integer cents, units, hashes, hidden-content and partial-publication controls across all 22 relations, run the focused command and record its expected RED result, then implement until GREEN. The oracle independently reads the workbook input cells, exported CSV bytes and manifest; it must not reuse adapter-normalization helpers for expected values. Write the safe synthetic `evidence/v1.0/workbooks/workbook-pack-parity.json` receipt with contract version, workbook/pack/manifest hashes, all 22 relation IDs, per-relation row/null/zero/cents/unit/hash dispositions, exact oracle command and overall PASS/BLOCKED; exclude absolute paths and cell contents. Per D-01, implement a read-only workbook adapter that checks archive members, metadata, comments, hyperlinks, defined names, hidden cells/sheets and external links using existing client-review security patterns; compare sheet/header set to the final v1 registry and preserve the original workbook bytes. Convert only declared input fields using Decimal for spreadsheet money boundaries and exact cents for CSV; do not trust cached formula results or silently coerce blank/zero. Export into a new private temporary canonical source-pack directory, then run the Phase 1 whole-pack validator; the weekly workflow consumes this validated pack rather than workbook cells directly. Return structured, sanitized workbook/sheet/row/field/issue/correction diagnostics for structural, key, relationship, unit/date, coverage and provenance failures. Reject partial export publication and protect an accepted immutable cut from overwrite. The XLSX is an editing surface, not source history.</action>
  <verify><automated>.venv\Scripts\python.exe -m unittest tests.test_operating_workbooks.WorkbookImportTests -v</automated></verify>
  <done>A valid workbook yields an intake-valid pack and the independent workbook→pack→manifest oracle proves exact rows, null/zero, cents, units, relation mapping and hashes for all 22 relations; its safe synthetic receipt rehashes and lists every relation with PASS; targeted invalid, divergent-manifest and hidden-content fixtures fail with location and reason, and original workbook bytes remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| XLSX/CSV → v1 intake | User-controlled archive cells, metadata and links cross into canonical records; exact shape and privacy validation precede output. |
| Generated public examples → ZIP | Only deterministic blank/synthetic rows and audited metadata cross to public material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-01 | Tampering | Workbook adapter | mitigate | Compare exact headers/relation set, reject formulas in inputs and archive extras, then call Phase 1 whole-pack validator before publishing. |
| T-04-02 | Information disclosure | XLSX metadata/comments/links | mitigate | Inspect all workbook surfaces, generate synthetic fixtures only, and reject private or external content at public-output boundary. |
| T-04-03 | Denial of service | Oversized XLSX | mitigate | Apply existing archive member and row/size limits before loading cells. |
| T-04-SC | Tampering | Dependency install | accept | Reuse already pinned workbook libraries; this plan adds no package install. |
</threat_model>

<verification>
Run the focused unittest module and validate both generated source packs with the implemented Phase 1 whole-pack CLI/API. Inspect the generated workbook sheet names, header rows, input validation, print metadata and synthetic labels in the test assertions. The independent workbook→pack→manifest oracle and 22-relation omission matrix are Phase 4 gates. Phase 5 owns fresh desktop Excel formula/cache/page evidence for the final delivered bytes; the approved UI contract identifies desktop Excel as the spreadsheet-engine support boundary and canonical CSV packs as the reader-neutral fallback.
</verification>

<success_criteria>
CLIENT-01 and D-01 are satisfied when both workbooks and CSV packs cover the final registry exactly, separate REVIEW/synthetic policy materials validate without claiming real approval, the example validates end to end, unknown/zero survive round-trip, and invalid workbook content cannot enter a cut.
</success_criteria>

<output>
Create `.planning/phases/04-offline-client-and-analyst-kit/04-01-SUMMARY.md` when done.
</output>
