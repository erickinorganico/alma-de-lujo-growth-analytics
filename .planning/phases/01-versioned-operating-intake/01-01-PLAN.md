---
phase: 01-versioned-operating-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alma/operating_contracts.py
  - scripts/operating_pack.py
  - tests/test_operating_contracts.py
  - client/source-packs/v1/blank/
  - client/source-packs/v1/blank/metadata.json
  - client/source-packs/v1/synthetic/
  - client/source-packs/v1/synthetic/metadata.json
autonomous: true
requirements: [DATA-01, DATA-04]
must_haves:
  truths:
    - "An analyst can initialize blank and visibly synthetic operating-v1 packs with exactly 22 ordered source CSVs, metadata, keys, units and provenance (D-02, D-03)."
    - "The same registry gives subsequent intake the exact grain, type, nullable, enum, PK, FK and coverage contract for each source (D-03, D-04)."
    - "Availability, unmet demand, budget allocations, stock movements and reservations have ingestible source definitions; absent coverage is not reported as zero (D-02, D-05)."
  artifacts:
    - path: alma/operating_contracts.py
      provides: "operating-v1 single source registry and metadata validator"
    - path: scripts/operating_pack.py
      provides: "blank/synthetic source-pack initializer"
    - path: client/source-packs/v1/synthetic/metadata.json
      provides: "public, explicitly synthetic pack metadata"
    - path: tests/test_operating_contracts.py
      provides: "literal header and metadata contract tests"
  key_links:
    - from: scripts/operating_pack.py
      to: alma/operating_contracts.py
      via: "registry driven headers, metadata and fixture rows"
    - from: alma/operating_contracts.py
      to: .planning/phases/01-versioned-operating-intake/01-CONTEXT.md
      via: "D-02 through D-05 and D-10/D-11 literal 22-file contract"
---

<objective>
Define the exact operating-v1 source and metadata interfaces and generate blank and synthetic packs from them.

Purpose: DATA-01 gives every later build one unambiguous aggregate source contract while D-01 preserves released v0.2/v0.3 evidence.
Output: Registry, initializer, public blank/example CSV packs, focused contract tests.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-versioned-operating-intake/01-CONTEXT.md
@.planning/phases/01-versioned-operating-intake/01-RESEARCH.md
@alma/interchange.py
@alma/warehouse.py
@tests/test_warehouse.py

<interfaces>
Existing v0.2 `alma/interchange.py::import_dataset(path)` requires `metadata.synthetic is True`; its `TABLE_COLUMNS` comes from `alma/warehouse.py` and refers to row-level order/customer tables. Do not route operating-v1 packs through it. `alma/warehouse.py` shows the local pattern for ordered table definitions and `STRICT` SQLite, but its exports and schema remain the v0.2 contract.

Create registry exports usable by plans 01-02 and 01-03: `CONTRACT_VERSION = "operating-v1"`, ordered `SOURCES` mapping, `SOURCE_NAMES`, `columns_for(name)`, `validate_metadata(metadata, *, allow_blank=False)`, `canonical_json(value) -> bytes`, and `source_contract(name)` exposing ordered field specifications, grain, PK/unique constraints, FKs, types, nullable flags, unit, and enums. Define one stable error type `OperatingContractError` carrying a machine-readable code and safe location. The registry must own metadata coverage vocabulary and identity preimage field names; later modules import these exports rather than duplicate them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the versioned source registry and metadata contract</name>
  <files>alma/operating_contracts.py, tests/test_operating_contracts.py</files>
  <behavior>
    - Literal expected headers of all 22 CSVs equal D-03, with exactly one trailing source_ref each; cash_events includes required economic_event_id and nullable supersedes_event_id in D-03 order.
    - Each source exposes type, nullable field, unit, grain, PK/unique key, FK, enum and provenance metadata; missing availability/ledger/allocation sources fail the test.
    - Metadata accepts D-02 valid private/synthetic shapes and rejects unknown versions, currency, coverage keys/states, invalid cutoff/zone/window, and build-time BLANK.
  </behavior>
  <action>Implement D-01 through D-04 and D-11 in `alma/operating_contracts.py` from the literal 22-row table in `01-CONTEXT.md`. Preserve exact field order and append `source_ref` once. Encode bounded token rules, strict integer/cent/basis-point and ISO date types, enum allowlists, nullable fields, grain, self-FK and relationship declarations, and coverage states. Use canonical UTF-8 JSON with sorted keys, compact separators and `allow_nan=False`; expose the named interface above. The contextual source table is authoritative where the research's 16-table proposal differs. Write literal contract tests first; avoid importing or changing v0.2 registry, workbook generator, and historical evidence.</action>
  <verify><automated>python -m unittest discover -s tests -p 'test_operating_contracts.py' -v</automated></verify>
  <done>All 22 exact headers and metadata rules are executable from one registry, including daily availability, unmet demand, budget allocations, stock ledgers, cash balance evidence and delivery cohorts in D-03/D-10/D-11; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate public blank and synthetic source packs</name>
  <files>scripts/operating_pack.py, client/source-packs/v1/blank/metadata.json, client/source-packs/v1/synthetic/metadata.json, tests/test_operating_contracts.py, and the all generated CSVs under the two source-pack directories in frontmatter</files>
  <behavior>
    - `--kind blank` and `--kind synthetic` generate deterministic metadata and all 22 CSVs in the requested new destination.
    - Blank CSVs have only headers and non-buildable BLANK metadata; synthetic metadata and every source_ref clearly identify synthetic origin.
    - Synthetic rows include cross-domain stock, reservation, availability, unmet-demand, budget-allocation, cash-balance and delivery-cohort links and a forecast-to-actual cash supersession chain, including 20 ordered / 18 received / 2 inspection / 16 accepted.
    - Re-running into an existing nonempty destination and writing a PRIVATE pack into client/ fail without changing bytes.
  </behavior>
  <action>Implement `scripts/operating_pack.py` as a local `init --kind blank|synthetic --output PATH` command using the registry for every header, typed fixture row, metadata key, and stable LF CSV serialization, per D-02, D-03, D-05, D-09 and D-11. Generate checked-in public packs under `client/source-packs/v1/blank/` and `client/source-packs/v1/synthetic/`. Use explicitly synthetic IDs/tokens and a fixed cutoff and zone. Mark source coverage honestly: the example can be `PARTIAL` where no comprehensive history exists; a genuinely observed empty fixture uses `ZERO`, while no fixture evidence uses `MISSING`. Include an opening stock movement, linked receipt/accepted movement, reservation events, SKU-day availability, recorded unmet demand, explicit purchase/expense budget allocations, reconciled cash with observed opening/closing balances, and aggregate delivery-cohort quality linkage, and one cash economic_event_id with a forecast superseded by an observed event. Refuse overwrite and private output into public client paths. Add tests that compare every generated file byte for byte with checked-in examples and assert no PII-like/free-text fields or row values.</action>
  <verify><automated>python -m unittest discover -s tests -p 'test_operating_contracts.py' -v</automated></verify>
  <done>Both packs are reproducible, use all 22 exact CSV headers, carry clear synthetic/blank provenance, and provide canonical identities for the next intake plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Registry to public CSV pack | Only blank or explicitly synthetic records may be generated under client/. |
| Command argument to filesystem | Output path and existing content are untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-01 | Information disclosure | Pack generator | mitigate | D-02/D-09 reject PRIVATE into client/ and test literal public generated rows for bounded tokens and synthetic markers. |
| T-01-02 | Tampering | Pack generator output | mitigate | Refuse nonempty destination and assert byte-stable re-generation; later plans bind source hashes. |
| T-01-03 | Spoofing | Synthetic example | mitigate | Metadata input_class and synthetic IDs must be explicit and tested. |
</threat_model>

<verification>
Run `python -m unittest discover -s tests -p 'test_operating_contracts.py' -v`; inspect literal 22-file headers and the generated metadata. Plan 01-03 runs the full v0.2/v0.3 regression suite.
</verification>

<success_criteria>
DATA-01 and the source-contract portion of DATA-04 are true: blank and synthetic packs are reproducible, complete as 22-file contracts, free of private content, and explicitly distinguish missing from zero coverage.
</success_criteria>

<output>
Create `.planning/phases/01-versioned-operating-intake/01-01-SUMMARY.md` when done.
</output>
