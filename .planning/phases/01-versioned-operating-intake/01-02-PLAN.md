---
phase: 01-versioned-operating-intake
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - alma/operating_interchange.py
  - alma/operating_workspace.py
  - models/operating_v1.sql
  - scripts/operating_build.py
  - tests/test_operating_intake.py
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04]
must_haves:
  truths:
    - "A valid synthetic or authorized private operating-v1 pack builds exactly one immutable private cut with 22 typed source relations (D-01, D-06)."
    - "Malformed, duplicate, conflicting, PII-bearing, or inconsistent input leaves no published database or metrics (D-04, D-06)."
    - "The cut manifest records stable identity, source and normalized hashes, cutoff, timezone, coverage, quality, row/key counts and relationship digest, preserving unknown versus zero (D-07)."
    - "Variant-day availability, recorded unmet demand, budget allocations, complete stock movements and reservation events are stored and related without fabricating transactions; cash forecast and actual rows share one economic identity and count only the active leaf (D-03, D-05, D-11)."
  artifacts:
    - path: alma/operating_interchange.py
      provides: "strict typed 22-file parser and whole-pack validation"
    - path: models/operating_v1.sql
      provides: "separate SQLite STRICT source schema"
    - path: alma/operating_workspace.py
      provides: "atomic private cut builder and manifest"
    - path: tests/test_operating_intake.py
      provides: "positive and fail-closed intake evidence"
  key_links:
    - from: alma/operating_interchange.py
      to: alma/operating_contracts.py
      via: "ordered source and metadata registry"
    - from: alma/operating_workspace.py
      to: models/operating_v1.sql
      via: "validated typed rows inserted with foreign keys enabled"
    - from: scripts/operating_build.py
      to: alma/operating_workspace.py
      via: "validate/build command"
---

<objective>
Validate a complete versioned source pack and publish one immutable aggregate SQLite cut with verifiable provenance.

Purpose: DATA-02 through DATA-04 require source failures and duplicate economic/stock facts to stop before an analytical workspace exists.
Output: Strict parser, operating schema, atomic builder, validation command, manifest and negative-control tests.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-versioned-operating-intake/01-CONTEXT.md
@.planning/phases/01-versioned-operating-intake/01-RESEARCH.md
@.planning/phases/01-versioned-operating-intake/01-01-SUMMARY.md
@alma/operating_contracts.py
@alma/interchange.py
@alma/warehouse.py
@alma/workspace.py
@models/schema.sql
@tests/test_security.py

<interfaces>
Consume the plan 01-01 exports `CONTRACT_VERSION`, `SOURCES`, `SOURCE_NAMES`, `columns_for`, `source_contract`, `validate_metadata`, `canonical_json`, and `OperatingContractError`. If that plan chose compatible signatures, adapt call sites to its actual exports; do not create a second registry. The existing v0.2 `build_workspace` writes source and metrics before final manifest, so this new operating builder must use its own stricter staging/publish boundary. `models/schema.sql` provides `STRICT` and FK/CHECK style, not tables to alter.

Export for plan 01-03: `parse_pack(path, *, private_root=None)` returning typed rows, metadata, raw source SHA-256 and safe diagnostics; `build_operating_workspace(pack_path, *, private_root=None)` returning `cut_id`, destination and manifest; `relationship_summary(connection)` returning deterministic row/key/FK, cash supersession and active-leaf summary/digest. The production default private root is repository `.local/`; test override is disposable and contains only synthetic data. `workspace.json` is the published manifest filename and `operating.sqlite3` is the database filename.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse and reject invalid whole packs before persistence</name>
  <files>alma/operating_interchange.py, tests/test_operating_intake.py</files>
  <behavior>
    - Exact 22-file synthetic pack parses to typed rows and preserves explicit zero, null, partial and missing coverage.
    - Unknown version, header reorder/duplicate/extra/PII column, ragged row, invalid date/integer/enum, undeclared blank, duplicate composite key, conflicting ID, and PII-like token/value fail with safe location/code before output exists.
    - A broken SKU, receipt, obligation, payment, budget-allocation, stock movement, reservation, availability, unmet-demand, cash-balance, or delivery-cohort relationship or orphan/self/cyclic/forked/cross-origin cash supersession fails; 20 ordered / 18 received / 2 inspection / 16 accepted passes.
  </behavior>
  <action>Implement D-02 through D-06 and D-11 in `alma/operating_interchange.py`. Read every declared CSV with UTF-8-SIG and `newline=''`, bound byte and row sizes, compare exact header order including repeated headers, and coerce only registry-declared integers and dates. Validate metadata coverage first, then row uniqueness and all cross-source relations in D-04/D-05. Implement typed null only for declared nullable fields; empty required cells fail. Validate one-to-one stock source links, reservation running balances, opening movements for complete movement coverage, budget origin allocation, daily exposure, D-10 observed-balance cash reconciliation plus D-11 required economic_event_id, self-FK, linear forecast-to-actual chain, same identity/origin, active-leaf-only sums and aggregate delivery-cohort links. A source marked MISSING/NOT_APPLICABLE must contain no rows; partial/missing coverage never becomes observed zero. PRIVATE inputs must resolve inside `.local/`; synthetic public input may come from `client/source-packs/v1/synthetic/`. Reject symlinks and traversal. Return stable codes/file/row/column without echoing private values. Write tests before implementation; do not import v1 through v0.2 synthetic-only interchange.</action>
  <verify><automated>python -m unittest discover -s tests -p 'test_operating_intake.py' -v</automated></verify>
  <done>Accepted rows are fully typed and relationally coherent before staging; every stated negative case fails without a workspace or private value in diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build strict private SQLite cut and provenance manifest</name>
  <files>models/operating_v1.sql, alma/operating_workspace.py, scripts/operating_build.py, tests/test_operating_intake.py</files>
  <behavior>
    - Valid synthetic pack builds a 22-table STRICT SQLite database under a new private cut ID with no order/customer tables and preserves the economic-event supersession chain.
    - The same exact pack/cutoff/zone yields the same ID; changing source bytes, cutoff, timezone or coverage changes it; an existing cut is never overwritten.
    - Manifest source byte hashes, typed-row digest, database hash, row/key counts, coverage, cash evidence status, cohort-link status and relationship digest match the built cut.
    - FK/business failures or publication errors clean staging and leave no published cut or metrics.
  </behavior>
  <action>Create `models/operating_v1.sql` with 22 source tables in FK insertion order using `STRICT`, PK/UNIQUE/FK/CHECK constraints matching D-03/D-04/D-11, including the cash self-FK and unique non-null supersedes_event_id, and no metric marts. In `alma/operating_workspace.py`, require SQLite 3.37+, call the plan's full-pack parser before persistence, derive D-07 canonical identity from metadata and raw source hashes, stage under private root, enable `PRAGMA foreign_keys=ON`, insert typed rows, run integrity/FK checks and the D-04/D-05 business reconciliations, compute deterministic row/key/relationship digest including D-11 economic identities, supersession edges, active leaves and origin bindings, then hash finalized SQLite bytes and atomically publish original CSVs, metadata, database and `workspace.json` only into an absent `.local/operating-cuts/<cut_id>/`. Reject any preexisting cut, even identical content, to preserve immutability. Expose `validate --input PATH` and `build --input PATH` in `scripts/operating_build.py`, with only safe diagnostic codes/locations. Tests assert no metrics or v0.2 row-level tables are created. Keep v0.2/v0.3 modules and evidence untouched per D-01/D-09.</action>
  <verify><automated>python -m unittest discover -s tests -p 'test_operating_intake.py' -v</automated></verify>
  <done>A valid pack yields exactly one private source workspace with stable complete manifest; invalid data or existing destination yields no new or changed accepted bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CSV/metadata to parser | User-controlled bytes may contain malformed rows, PII, forged keys and paths. |
| Validated rows to SQLite | Application checks must be reinforced by typed PK/FK/CHECK constraints. |
| Staging to accepted cut | A partial build must never become a trusted published workspace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-01-04 | Tampering | Source rows and IDs | mitigate | D-04/D-06 exact header, key/FK, event-link, D-11 linear cash supersession and source-hash validation; stage before publish. |
| T-01-05 | Information disclosure | Parser diagnostics and paths | mitigate | D-06 reject PII-like fields/values and path escape; emit codes/locations without cell values. |
| T-01-06 | Denial of service | CSV parser | mitigate | Bound file/row sizes before parsing and test oversized rejection. |
| T-01-07 | Elevation of privilege | Existing/private output | mitigate | D-06 refuse symlinks, traversal and preexisting cut; restrict default outputs to `.local/`. |
| T-01-08 | Repudiation | Cut provenance | mitigate | D-07 bind original bytes, metadata, typed rows, DB and relationships into the manifest. |
</threat_model>

<verification>
Run `python -m unittest discover -s tests -p 'test_operating_intake.py' -v`. Inspect SQLite `PRAGMA foreign_key_check` and verify the 22 source tables have `STRICT` definitions. Plan 01-03 runs full regression after archive/restore is implemented.
</verification>

<success_criteria>
DATA-02, DATA-03 and canonical-source DATA-04 are achieved: every accepted cut has stable provenance and all 22 source families share validated identities and D-10 cash/cohort evidence gates and D-11 active economic-event chains; malformed, duplicate, PII or conflicting inputs cannot publish.
</success_criteria>

<output>
Create `.planning/phases/01-versioned-operating-intake/01-02-SUMMARY.md` when done.
</output>
