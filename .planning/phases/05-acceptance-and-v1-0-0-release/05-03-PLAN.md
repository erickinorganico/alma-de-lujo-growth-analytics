---
phase: 05-acceptance-and-v1-0-0-release
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - scripts/package_release_v1.py
  - scripts/audit_release_v1.py
  - tests/test_release_v1.py
  - client/v1/release-manifest.json
  - client/v1/acceptance-summary.json
  - docs/RUNBOOK-v1.md
  - docs/ACCEPTANCE-v1.md
  - docs/RELEASE-v1.md
  - .local/releases/Alma_OS_v1.0.0*
autonomous: true
requirements: [REL-04]
must_haves:
  truths:
    - "The exact `v1.0.0` tagged commit produces a deterministic `Alma_OS_v1.0.0.zip` containing the offline portal, runbook, blank/example materials and safe acceptance summary, with no private/native response content (D-07, D-08)."
    - "An exhaustive audit of the extracted ZIP requires `member_set = manifest.entries + {release-manifest.json}`, verifies every non-manifest member hash, binds the manifest only through the outer ZIP/external sidecar, and checks workbook internals, binary inventory, offline links and privacy/secret constraints before publication (D-07, D-08)."
    - "The checksum sidecar matches the published ZIP and a separate acceptance sidecar binds tag, commit, ZIP hash, CI, native, Excel, archive and gate evidence without recursive hashing (D-08)."
    - "A fresh extraction works offline on the supported local boundary and all documented commands/links resolve; repeated packaging from the same tag yields the same bytes."
    - "Published acceptance states EXT-01 real adoption `UNKNOWN`, EXT-02 policy `UNKNOWN` and EXT-03 Pilates-socks hypothesis `REVIEW`, and retains `execution=PROHIBITED` (D-09, D-10)."
  artifacts:
    - path: scripts/package_release_v1.py
      provides: "Deterministic tag-bound v1.0.0 public release builder"
    - path: scripts/audit_release_v1.py
      provides: "Exhaustive ZIP/member/binary/link/privacy/provenance auditor"
    - path: docs/RUNBOOK-v1.md
      provides: "Client and analyst installation, weekly operation, recovery and limits"
    - path: docs/ACCEPTANCE-v1.md
      provides: "Human-readable gate evidence and unresolved external acceptance"
    - path: .local/releases/Alma_OS_v1.0.0.acceptance.json
      provides: "Non-recursive exact release sidecar keyed to commit and ZIP SHA-256"
  key_links:
    - from: scripts/package_release_v1.py
      to: scripts/package_client_v1.py
      via: "Phase 4 verified public kit inventory plus the exact Phase 5 reviewed workbook bytes"
    - from: client/v1/release-manifest.json
      to: scripts/audit_release_v1.py
      via: "Non-recursive entries excluding the manifest itself; exact member-set equality and payload hashes"
    - from: .local/releases/Alma_OS_v1.0.0.acceptance.json
      to: .local/release-evidence/v1.0.0-ci-reproducibility.json
      via: "Exact same-SHA CI/archive/native/Excel receipt hashes and tag/commit/ZIP identity"
    - from: docs/RUNBOOK-v1.md
      to: run.ps1
      via: "Exact installed offline and analyst weekly commands"
---

<objective>
Create, exhaustively audit and publish the reproducible Alma OS v1.0.0 offline release from its exact reviewed commit.

Purpose: REL-04 and D-07 through D-10 require a usable final artifact whose content, provenance, privacy and limitations are independently inspectable after extraction.
Output: Deterministic ZIP, checksum and acceptance sidecars, exhaustive auditor, runbook, acceptance record, tag and GitHub release.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-acceptance-and-v1-0-0-release/05-CONTEXT.md
@.planning/phases/05-acceptance-and-v1-0-0-release/05-RESEARCH.md
@.planning/phases/04-offline-client-and-analyst-kit/04-03-SUMMARY.md
@.planning/phases/05-acceptance-and-v1-0-0-release/05-01-SUMMARY.md
@.planning/phases/05-acceptance-and-v1-0-0-release/05-02-SUMMARY.md
@scripts/package_client_v1.py
@scripts/audit_release.py
@scripts/github-personal.ps1
@docs/GITHUB-ACCESS.md
@client/v1/workbook-manifest.json
@evidence/v1.0/regression-acceptance.json
@evidence/v1.0/two-week-native.json
@.local/release-evidence/v1.0.0-ci-reproducibility.json

<interfaces>
`scripts/package_release_v1.py` accepts `--ref v1.0.0|COMMIT --output .local/releases/Alma_OS_v1.0.0.zip --checksum ... --acceptance ...`. It must build from a fresh `git archive` of that exact ref, call the Phase 4 public-kit builder in the archive, require the exact committed Phase 5 workbook hashes/receipts, and normalize ZIP order, paths, permissions and timestamps. It writes an inner `acceptance-summary.json` that excludes the outer ZIP hash, then writes external checksum and acceptance sidecars after the ZIP exists.

The external acceptance sidecar is intentionally not a tracked input to the tagged commit: it records the final ZIP hash, the inner release-manifest hash and the ignored same-SHA CI sidecar hash without creating a self-reference. It is a release asset alongside the ZIP and `.sha256`. The tracked `client/v1/acceptance-summary.json` contains deterministic gate identities, committed receipt hashes, limitations and EXT states but no mutable CI run IDs, private paths, task response bodies or outer archive hash. The inner `release-manifest.json` never lists or hashes itself: its `entries` cover every other ZIP member, the auditor requires `member_set = entries + {release-manifest.json}`, and only the outer ZIP hash plus external acceptance sidecar bind the manifest bytes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the deterministic release packager and exhaustive extracted-ZIP auditor</name>
  <files>scripts/package_release_v1.py, scripts/audit_release_v1.py, tests/test_release_v1.py</files>
  <behavior>
    - Packaging the same committed ref twice yields byte-identical ZIPs with exact normalized member order, timestamps, permissions and paths.
    - The manifest `entries` allowlist every payload file except `release-manifest.json`, with SHA-256, size, media/type, source, synthetic/blank classification and client purpose; the auditor requires `member_set = entries + {release-manifest.json}` and never asks the manifest to hash itself.
    - Every local HTML/Markdown link resolves inside the extracted tree without traversal; no runtime remote asset or server is required.
    - Case-insensitive duplicate names, absolute/traversal/backslash/device paths, symlinks, excessive member/count/ratio/size, unexpected member, changed byte/hash, broken/escaping/external URL and version mismatch fail.
    - Workbook XML/properties/comments/links/defined names/hidden content/macros and every PDF/image/binary member are inventoried and checked; private cuts, native requests/responses/traces, register events, `.local`, secrets and PII-like content fail.
  </behavior>
  <action>Implement the packager, auditor and tests for D-07 and D-08 without producing the final tracked manifest in this task. Define the explicit literal payload allowlist contract and a manifest-generation mode whose normalized POSIX `entries` exclude `release-manifest.json` itself and contain per-member hashes for every other ZIP member; do not copy a directory recursively. The auditor must require exact `member_set = entries + {release-manifest.json}`, reject a manifest self-entry, and bind the manifest's own bytes only through the outer ZIP SHA-256 and external acceptance sidecar. Use fixed ZIP timestamps/permissions/compression/order and reject any source symlink. In `scripts/audit_release_v1.py`, validate central-directory and extracted paths before extraction, duplicate/case-fold names, compression and total/member limits, manifest completeness, actual payload hashes, local links, text encoding/content and absence of remote runtime dependencies. Inspect XLSX ZIP members, metadata, cells/formulas/comments/hyperlinks/defined names/hidden surfaces/external relationships/macros; compare delivered book bytes to the Excel and workbook manifests. Explicitly inventory and allowlist any PDF/image/binary and inspect metadata/content rather than relying on `audit_release.py` suffix skips. Scan text and XML for project secret/credential/private-path/PII patterns with documented false-positive handling and assert all business fixtures are BLANK or SYNTHETIC. Retain the existing v0.3 audit as a separate regression command. Tests use complete temporary payload fixtures and create mutations for every behavior above, including manifest self-entry, missing/extra payload and changed manifest bytes. Task 2 owns generation and verification of the final `client/v1/release-manifest.json` only after every real documentation and acceptance payload byte exists.</action>
  <verify><automated>.venv/Scripts/python.exe -m unittest tests.test_release_v1 -v</automated></verify>
  <done>The packager/auditor contract produces byte-identical temporary builds, enforces payload entries plus a non-self-referential manifest, and rejects every malicious, self-referential or stale fixture; no final tracked release manifest is emitted before Task 2 payloads exist.</done>
</task>

<task type="auto">
  <name>Task 2: Write the final operating runbook and evidence-backed acceptance record</name>
  <files>client/v1/acceptance-summary.json, docs/RUNBOOK-v1.md, docs/ACCEPTANCE-v1.md, docs/RELEASE-v1.md, client/v1/release-manifest.json, tests/test_release_v1.py</files>
  <action>Per D-08 through D-10, first write a Spanish task-first runbook covering prerequisites, archive extraction, offline entrypoint, blank/example distinction, private-copy rules, exact `run.ps1 weekly` analyst command, stage receipts, actual native continuation, owner decision boundary, next-cut carry/closure, export/restore, backup, recovery, package verification and safe update/uninstall. Copy every command and flag from actual `--help`, and test those literals against the implemented CLIs. Write `ACCEPTANCE-v1.md` from committed machine-readable Phase 5 receipts: show each deterministic/local REL gate, exact command, status, evidence/hash, platform, limitations and remediation; distinguish deterministic CI validation from actual local native and Excel execution. Generate a compact tracked `client/v1/acceptance-summary.json` with committed receipt digests, package scope, software version and external states `EXT-01=UNKNOWN`, `EXT-02=UNKNOWN`, `EXT-03=REVIEW`; represent live remote CI/publication as `EXTERNAL_SIDECAR_REQUIRED` rather than embedding future run IDs or claiming PASS before SHA A is frozen. Exclude absolute paths, private inputs, query/response prose and runtime credentials. The later external release acceptance sidecar carries the actual same-SHA CI/publication proof. Write `RELEASE-v1.md` with final behavior, installation, verification commands, compatibility, known external gates and artifact names. State that Pilates socks are a hypothesis under review, historical/synthetic observations are not real business performance, arithmetic is management analytics rather than fiscal advice, and all external business execution is prohibited. After every documentation and `acceptance-summary.json` byte is final, run the Task 1 manifest-generation mode to write `client/v1/release-manifest.json`. Its entries must hash the exact final payload set and exclude only the manifest itself. Run a staged-source manifest check that requires every declared source payload to exist with the recorded bytes, rejects any missing/extra payload and asserts the future ZIP member equation `member_set = entries + {release-manifest.json}`. Regenerate the manifest after any payload repair. Commit the final docs, acceptance summary and manifest together before Task 3 freezes or tags a commit.</action>
  <verify><automated>.venv/Scripts/python.exe -m unittest tests.test_release_v1.ReleaseDocumentationTests -v &amp;&amp; .venv/Scripts/python.exe scripts/audit_release_v1.py check-manifest --manifest client/v1/release-manifest.json --source-root . --require-version 1.0.0</automated></verify>
  <done>The runbook is executable by a new client/analyst offline, acceptance claims map to actual receipt hashes, no external or business-impact claim is upgraded, and the committed non-self-referential release manifest was generated last and hashes every final payload byte.</done>
</task>

<task type="auto">
  <name>Task 3: Tag, rebuild, audit and publish the exact v1.0.0 assets</name>
  <files>.local/releases/Alma_OS_v1.0.0.zip, .local/releases/Alma_OS_v1.0.0.zip.sha256, .local/releases/Alma_OS_v1.0.0.acceptance.json</files>
  <action>Require completed `05-01-SUMMARY.md` and `05-02-SUMMARY.md`, plus committed Task 1–2 packager/auditor/runbook/acceptance/final-manifest inputs; `05-03-SUMMARY.md` is deliberately created only after publication and is never a tag or ZIP prerequisite. Require the full deterministic verifier, actual sanitized v1 native receipt with query-trace chain, exact Excel/rendered-page receipts and the pre-merge rehearsal to pass. Use repository-local Git identity and `scripts/github-personal.ps1` for every `gh` call, confirm the wrapper reports `erickinorganico`, push the authorized branch, create/update the final PR and merge only when its checks are green. Fetch the merged commit, designate it final frozen SHA A and make no tracked mutation afterward. Through the wrapper, require the merge-triggered Windows and Ubuntu jobs to be terminal and green for `headSha=A`; this preliminary read is not an evidence-sidecar writer. Create the annotated tag `v1.0.0` locally on exactly A but do not push it. Now run exactly once `.venv/Scripts/python.exe scripts/verify_release_archive.py prove --ref v1.0.0 --output .local/release-archive-final --archive-receipt .local/release-evidence/v1.0.0-archive-final.json --remote-sidecar .local/release-evidence/v1.0.0-ci-reproducibility.json`; it must rebuild the tag archive, revalidate both existing A jobs and downloaded receipts, and create the canonical final archive receipt plus CI sidecar. If it fails, publish nothing. Never switch global GitHub auth or use global `gh auth setup-git`. From the locally verified tag, run `scripts/package_release_v1.py` twice in separate clean archive directories and require identical bytes and the committed final manifest contract, run both v1 and legacy audits, extract the chosen ZIP to a new directory, rehash every member, open/parse the offline entrypoint and run documented smoke/help checks without network. Write lowercase-hex `Alma_OS_v1.0.0.zip.sha256` and external `Alma_OS_v1.0.0.acceptance.json` binding tag, A, ZIP/checksum, inner manifest hash, the canonical ignored CI-sidecar hash, complete gate statuses and hashes of native/Excel/archive receipts; do not include private responses or query content. Only then push the tag and publish a GitHub release through `scripts/github-personal.ps1 release create v1.0.0` using `docs/RELEASE-v1.md` and exactly these three assets. Implement `audit_release_v1.py verify-published` so every release view/download operation is delegated to the supplied wrapper; it accepts `--ci-sidecar`, rehashes that local ignored file against the downloaded acceptance sidecar, requires its resolved proof SHA and both downloaded Windows/Ubuntu receipt `headSha` fields equal tag target A, and rehashes both receipts. It then reads the release back, downloads all three assets into a fresh directory, requires exact names/sizes, reruns the checksum/acceptance/extracted-ZIP audit on downloaded bytes, compares tag target and sidecar commit to A, and fails on any mismatch. All post-publication archive verification uses `prove --check-existing`, which snapshots and preserves the canonical receipt/sidecar bytes. Attach the created PR to the Codex task. If any prerequisite, final-A archive/CI proof, rebuild, extracted audit, upload or readback differs, do not publish or delete/replace a good prior release; retain BLOCKED evidence for correction.</action>
  <verify><automated>.venv/Scripts/python.exe scripts/verify_release_archive.py prove --check-existing --ref v1.0.0 --archive-receipt .local/release-evidence/v1.0.0-archive-final.json --remote-sidecar .local/release-evidence/v1.0.0-ci-reproducibility.json &amp;&amp; .venv/Scripts/python.exe scripts/audit_release_v1.py --zip .local/releases/Alma_OS_v1.0.0.zip --checksum .local/releases/Alma_OS_v1.0.0.zip.sha256 --acceptance .local/releases/Alma_OS_v1.0.0.acceptance.json --ci-sidecar .local/release-evidence/v1.0.0-ci-reproducibility.json --require-version 1.0.0 &amp;&amp; .venv/Scripts/python.exe scripts/audit_release_v1.py verify-published --tag v1.0.0 --github-wrapper scripts/github-personal.ps1 --ci-sidecar .local/release-evidence/v1.0.0-ci-reproducibility.json --download-dir .local/releases/readback-v1.0.0 --require-version 1.0.0</automated></verify>
  <done>The tag resolves to final SHA A; a fresh clean archive and both downloaded Windows/Ubuntu receipts prove `headSha=A`; the ignored CI sidecar rehashes against the acceptance sidecar; the GitHub `v1.0.0` release under `erickinorganico` points at A; its three downloaded assets reverify exactly; and the extracted kit works offline with no private/native response/secret/PII content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Tagged Git tree → release builder | Only reviewed committed public inputs may enter the deterministic package. |
| ZIP central directory → extracted filesystem | Malicious paths, duplicates, links or decompression behavior must be rejected before writes. |
| Local release assets → GitHub release | Tag, commit, account, uploaded bytes and readback hashes must remain identical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05-11 | Tampering | ZIP/member/checksum | mitigate | D-07/D-08 explicit allowlist, normalized deterministic build, per-member/outer hashes, second rebuild and downloaded-asset recheck. |
| T-05-12 | Information disclosure | Public release content | mitigate | Inspect text and binary surfaces; reject private paths/cuts/native bodies/registers, secrets and PII-like content. |
| T-05-13 | Elevation of privilege | ZIP extraction | mitigate | Validate traversal, absolute/device/case-fold paths, symlinks, counts/sizes/ratios before extracting into a new directory. |
| T-05-14 | Spoofing | GitHub account/tag/release | mitigate | Use isolated wrapper, require `erickinorganico`, annotated tag exact commit, release readback and downloaded asset hashes. |
| T-05-15 | Repudiation | Release acceptance | mitigate | Non-recursive sidecar binds tag, commit, ZIP, CI and every prerequisite receipt hash; missing gates remain BLOCKED. |
| T-05-16 | Elevation of privilege | Advisory analytics | mitigate | D-10 package/runbook preserve owner approval and `execution=PROHIBITED`; no business API or action exists. |
| T-05-SC | Tampering | Dependency install | accept | No new dependency or provider is added; release rebuild uses the audited pinned set from the tag. |
</threat_model>

<verification>
Run release unit tests and documentation-command/link tests; commit all tracked implementation inputs plus the completed 05-01/05-02 summaries; merge, freeze final SHA A, and require A's clean archive and Windows/Ubuntu checks. Tag only A, build twice, audit/extract/smoke test, publish via the isolated wrapper, download all three assets and verify hashes again. Create `05-03-SUMMARY.md` only afterward. Confirm the public package and GitHub assets contain only blank/synthetic material and safe acceptance metadata.
</verification>

<success_criteria>
REL-04 and the phase goal are achieved when the exact reviewed `v1.0.0` commit reproducibly yields the published ZIP, checksum and acceptance sidecar; the downloaded extracted package works offline, passes exhaustive content/privacy/link/hash audits, carries usable runbook/evidence, and makes no unsupported external claim.
</success_criteria>

<output>
Create `.planning/phases/05-acceptance-and-v1-0-0-release/05-03-SUMMARY.md` when done.
</output>
