---
phase: 05-acceptance-and-v1-0-0-release
plan: 01
type: execute
wave: 1
depends_on: [04-03]
files_modified:
  - AGENTS.md
  - tests/fixtures/release_v1/two_week_scenario.json
  - tests/test_v1_release_acceptance.py
  - scripts/verify_v1.py
  - evidence/v1.0/regression-acceptance.json
  - evidence/v1.0/two-week-native.json
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "Every released v0.2/v0.3 gate and the v1 positive, partial, tamper, duplicate and double-count controls pass together, or the release is BLOCKED (D-01)."
    - "Two synthetic weekly cuts have different cutoffs, cut IDs, source hashes and observations; exact replay is proven separately and cannot impersonate week two (D-02)."
    - "A week-one reviewed synthetic decision retains its original evidence anchor in week two and reaches valid closure, while a second branch proves deterministic stale handling (D-02)."
    - "Every Phase 3 analyst request is executed by an actual native Codex role and reviewed by a different native agent; fixtures alone never satisfy the live gate (D-03)."
    - "Repository instructions route aggregate-v1 cycles to RUN-NATIVE-CYCLE-v1.md and native-cycle-v1.roles.json, while RUN-NATIVE-CYCLE.md is explicitly historical regression-only (D-03)."
    - "The public receipt exposes safe hashes and states while complete native responses, traces, registers and filled cuts stay below ignored `.local/` paths; EXT-01..03 remain unclaimed (D-03, D-09)."
  artifacts:
    - path: AGENTS.md
      provides: "Authoritative aggregate-v1 versus historical native-cycle routing"
    - path: tests/test_v1_release_acceptance.py
      provides: "Release-level invariant and adversarial matrix over Phase 1–4 interfaces"
    - path: scripts/verify_v1.py
      provides: "Fail-closed deterministic/live v1 acceptance runner and receipt validator"
    - path: evidence/v1.0/regression-acceptance.json
      provides: "Machine-readable historical plus deterministic v1 gate results"
    - path: evidence/v1.0/two-week-native.json
      provides: "Sanitized actual native task/review and decision-continuity receipt"
  key_links:
    - from: scripts/verify_v1.py
      to: alma/weekly.py
      via: "Phase 4 one-command adapter over final Phase 1–3 cut, mart, task and register interfaces"
    - from: AGENTS.md
      to: agents/RUN-NATIVE-CYCLE-v1.md and agents/native-cycle-v1.roles.json
      via: "Explicit aggregate-v1 route before any live task dispatch; historical runbook restricted to regression"
    - from: tests/test_v1_release_acceptance.py
      to: tests/fixtures/release_v1/two_week_scenario.json
      via: "Literal expected cut changes, cents, units, coverage and closure/stale outcomes"
    - from: evidence/v1.0/two-week-native.json
      to: .local/v1-acceptance
      via: "Hashes and safe runtime identifiers only; private response bodies are excluded"
---

<objective>
Prove the complete two-week analytical and native decision chain while retaining every historical release gate.

Purpose: REL-01 and D-01 through D-04 require observable current behavior, adversarial failure evidence and a real independent native review rather than a file-count claim.
Output: Deterministic release tests, unified verifier, actual two-week native run, and sanitized acceptance receipts.
</objective>

<execution_context>
@C:/Users/erick/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/erick/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-acceptance-and-v1-0-0-release/05-CONTEXT.md
@.planning/phases/05-acceptance-and-v1-0-0-release/05-RESEARCH.md
@.planning/phases/01-versioned-operating-intake/01-03-SUMMARY.md
@.planning/phases/02-reconciled-decision-metrics/02-03-SUMMARY.md
@.planning/phases/03-governed-weekly-decision-cycle/03-03-SUMMARY.md
@.planning/phases/04-offline-client-and-analyst-kit/04-03-SUMMARY.md
@agents/RUN-NATIVE-CYCLE-v1.md
@agents/native-cycle-v1.roles.json
@scripts/run_weekly_cycle.py
@scripts/verify_v2.py
@scripts/check_scope_v2.py
@scripts/verify_client_v3.py
@scripts/build_client_system.py

<interfaces>
Read the completed Phase 1–4 summaries before editing and bind to their actual exports. The expected public seams are the Phase 1 immutable `workspace.json`/`operating.sqlite3` cut and archive verifier, Phase 2 canonical report/mart registry/reconciliation receipt, Phase 3 v1 task/dispatch/packet/register adapters, and Phase 4 `alma.weekly` entrypoint. Do not reimplement formulas, event identity, native response validation or register transitions in this plan.

`scripts/verify_v1.py` must expose three explicit modes: `deterministic --output PATH`, `prepare-live --output .local/v1-acceptance`, and `finalize-live --run .local/v1-acceptance --summary evidence/v1.0/two-week-native.json`. Deterministic mode is suitable for CI and never reports live inference. Prepare-live builds immutable week-one/week-two synthetic cuts and their actual aggregate-v1 task requests from `agents/native-cycle-v1.roles.json`. Finalize-live accepts only Phase 3-validated request, response, registered-query-trace, dispatch, frozen reviewer-request, review, packet, journal and register hashes already written by `scripts/run_weekly_cycle.py` under `agents/RUN-NATIVE-CYCLE-v1.md`. The historical runbook/process CLI is never a v1 execution seam.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the independent two-week invariant and adversarial matrix</name>
  <files>tests/fixtures/release_v1/two_week_scenario.json, tests/test_v1_release_acceptance.py</files>
  <behavior>
    - A valid week one and a materially changed week two build, verify, export/restore, reconcile and yield distinct cutoff/cut/source/report hashes.
    - Replaying exact week one is idempotent at canonical row/export/relationship level and never counts as week two; SQLite binary equality is not required.
    - Known cents/units exactly match an independent integer/Decimal oracle for costs, purchase receipt/inspection/acceptance, stock/reservations/availability, obligations/payments/cash, budget allocations, product learning, quality/readiness and loans.
    - Removing eligible cost, settlement, stock movement, availability or cohort coverage leaves known sums known and makes dependent complete values UNKNOWN/REVIEW; observed zero stays zero.
    - Mutated source/manifest/report bytes, broken FK, duplicate key, conflicting economic event, receipt-plus-movement double count, obligation/payment double count and invalid closure anchor fail before publication or owner-ready state.
    - A synthetic fixture decision carries its original source/report/packet hashes into week two; valid exact evidence closes one branch and an overdue unclosed copy becomes STALE; duplicate ID, changed anchor and forged closure fail.
  </behavior>
  <action>Per D-01, D-02 and D-04, write the tests before the runner. Define a compact scenario manifest containing the two ISO cutoffs, expected changed input fields, exact source totals, expected mart totals, coverage states, one closure rule and one stale due date; generate full source packs through the final Phase 1 registry rather than checking in a second 22-CSV contract. Exercise the actual Phase 1 parser/archive, Phase 2 marts/report/reconciliation, Phase 3 response/register validators and Phase 4 weekly adapter in disposable directories. Recompute all accepted money with integer cents/`Decimal`, all inventory with integers and event identities, and compare canonical exports and relationship digests after restore. Inject every negative control in isolation and assert no accepted metrics, packet or decision state appears. Use schema-valid native response fixtures only to test validators, mark them `TEST_FIXTURE`, and assert they cannot satisfy the live-evidence predicate. Preserve EXT-01 `UNKNOWN`, EXT-02 `UNKNOWN` and EXT-03 `REVIEW`; the Pilates-socks observations in this synthetic fixture never establish a winner.</action>
  <verify><automated>.venv/Scripts/python.exe -m unittest tests.test_v1_release_acceptance -v</automated></verify>
  <done>The complete deterministic matrix passes with exact independent totals, distinct week identities, closure/stale continuity and all stated fail-closed controls; no fixture is classified as a live native run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the unified fail-closed release acceptance runner</name>
  <files>AGENTS.md, scripts/verify_v1.py, evidence/v1.0/regression-acceptance.json, tests/test_v1_release_acceptance.py</files>
  <behavior>
    - Deterministic mode runs the full unittest suite, `verify_v2.py`, `check_scope_v2.py`, `verify_client_v3.py check`, historical and v1 portal checks, then the new v1 matrix, and records each exact command/result/hash.
    - Any skipped, missing, stale or failed required gate produces overall BLOCKED/FAIL and nonzero exit; a documentation flag cannot upgrade it.
    - Acceptance JSON records commit, interpreter/platform, input/output hashes, counts, limits and external-gate states without absolute private paths or response prose.
    - Live preparation/finalization rejects fabricated IDs, `mode != live`, response/hash mismatch, same analyst/reviewer identity, stale evidence, changed analyst after review dispatch, missing challenge trace or external execution claim.
  </behavior>
  <action>Before implementing or dispatching any aggregate-v1 live work, update the repository-local `AGENTS.md` analytical-cycle rule so aggregate-v1 explicitly follows `agents/RUN-NATIVE-CYCLE-v1.md`, `agents/native-cycle-v1.roles.json` and `scripts/run_weekly_cycle.py`, while `agents/RUN-NATIVE-CYCLE.md` is explicitly limited to historical v0.2 regression. Preserve every unrelated instruction. Add a deterministic contract assertion that this routing matches the completed Phase 3 v1 files. Then implement D-01, D-03, D-04, D-09 and D-10 in `scripts/verify_v1.py`. Use subprocess argument arrays, explicit timeouts and structured captured results; call existing v0.2/v0.3 scripts unchanged and add `scripts/build_client_system.py --check` plus the Phase 4 v1 portal/package checks. Generate `evidence/v1.0/regression-acceptance.json` atomically with PASS/FAIL/BLOCKED per gate, candidate commit, canonical source/mart/decision digests and bounded diagnostics. In live modes, enforce the final Phase 3 v1 receipt schema and rehash request, response, registered-query trace, dispatch, frozen reviewer request, reviewer response/challenge, packet, journal and register anchors from `.local/`; compare `query_trace_sha256` in the parent receipt, analyst acceptance event and reviewer request, and reject any mismatch. Never accept a caller-written boolean or a test fixture marker. Emit a sanitized relative summary with task IDs, roles/models, start/completion timestamps, request/response/query-trace/dispatch/reviewer-request/packet/register hashes, reviewer identity difference, challenge disposition and terminal state, while excluding response text, query content, private inputs and absolute paths. The deterministic runner may validate a recorded live summary's schema and source anchors but must state that CI did not execute the model.</action>
  <verify><automated>.venv/Scripts/python.exe scripts/verify_v1.py deterministic --output evidence/v1.0/regression-acceptance.json</automated></verify>
  <done>`AGENTS.md` unambiguously routes aggregate-v1 to the v1 runbook/roles and historical regression to the old runbook; the unified runner fails closed and produces a complete deterministic receipt only after historical and v1 commands all pass; its live validator cannot be satisfied by fixture/fabricated metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Execute actual native review and prove decision continuity across two cuts</name>
  <files>evidence/v1.0/two-week-native.json</files>
  <action>Run `.venv/Scripts/python.exe scripts/verify_v1.py prepare-live --output .local/v1-acceptance` and follow `agents/RUN-NATIVE-CYCLE-v1.md` plus `agents/native-cycle-v1.roles.json` on every generated Phase 3 v1 request. Dispatch merchandising/finance work to native Terra, commerce/returns to native Luna, growth/market work to native Sol, and each review to a separate native Astra task using the exact v1 role identifiers; if the runtime's exact model catalog differs, use only an authorized native equivalent allowed by the v1 contract and record the actual runtime model. Each analyst inspects bound current-cut evidence, executes at least one registered read-only mart query, writes a schema-valid query trace with exact result pointers and row count, and submits a schema-valid response with exact JSON pointers, guardrail, population, window, closure rule, approval boundary and `execution=PROHIBITED`. The parent records actual task ID/model/effort/timestamps plus canonical request, response, query-trace and dispatch hashes, then submits only through `scripts/run_weekly_cycle.py`. Dispatch the distinct Astra reviewer only from the frozen reviewer request that binds every accepted response, query-trace and dispatch digest; the reviewer checks values, denominators, temporal fit and meaning. Register one clearly labelled synthetic acceptance-fixture decision from a reviewed packet, without claiming real owner adoption or executing an action; carry its original source/report/packet/register anchors to the distinct week-two cut and close it only through declared exact evidence. Also prove the separate stale branch from Task 1. Finish with `.venv/Scripts/python.exe scripts/verify_v1.py finalize-live --run .local/v1-acceptance --summary evidence/v1.0/two-week-native.json`; keep full requests/responses/query traces/registers under `.local/`. If any actual dispatch, query trace, independent review, frozen digest, packet or closure anchor is absent, leave the receipt BLOCKED and do not proceed to release.</action>
  <verify><automated>.venv/Scripts/python.exe scripts/verify_v1.py finalize-live --run .local/v1-acceptance --summary evidence/v1.0/two-week-native.json</automated></verify>
  <done>The sanitized receipt rehashes every real request/response/query-trace/dispatch/reviewer-request/packet/register link, proves different analyst/reviewer identities and week-one-to-week-two closure, retains the stale negative branch, contains no private prose/query/data and leaves EXT-01..03 unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Synthetic acceptance inputs → canonical cut/marts | Adversarial rows can duplicate money/units or collapse unknown into zero. |
| Native runtime → Phase 3 process bridge | Model output and parent receipts are untrusted until hashes, references and identities validate. |
| Private live run → committed receipt | Only safe provenance and hashes may cross; response prose and filled cuts remain local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05-01 | Tampering | Source/mart/register chain | mitigate | D-01/D-02/D-04 independent totals, byte hashes, restore comparisons, unique events and forged closure controls. |
| T-05-02 | Spoofing | Native task/reviewer identity | mitigate | D-03 actual runtime IDs, `mode=live`, response hashes and distinct analyst/reviewer identity; fixtures are rejected for live acceptance. |
| T-05-03 | Repudiation | Two-week decision continuity | mitigate | Preserve original cut/report/packet/register anchors and record append-only carry/closure/stale events. |
| T-05-04 | Information disclosure | Sanitized public receipt | mitigate | Allowlist safe fields, reject absolute paths/prose, keep full run under ignored `.local/`. |
| T-05-05 | Elevation of privilege | Advisory recommendation | mitigate | D-10 require approval boundary and `execution=PROHIBITED`; synthetic fixture entry cannot imply owner adoption. |
| T-05-SC | Tampering | Dependency install | accept | This plan uses Python standard library and already pinned project dependencies; no install or pin change occurs. |
</threat_model>

<verification>
Run the focused v1 acceptance tests, the unified deterministic verifier, actual native preparation/dispatch/finalization, and then rerun deterministic verification so the recorded live summary is validated against unchanged source/report anchors. Inspect the committed JSON to confirm it contains no response bodies, private paths, customer data or upgraded EXT-01..03 state.
</verification>

<success_criteria>
REL-01 is achieved when all v0.2/v0.3 gates and the v1 adversarial matrix pass, two genuinely different cuts preserve an exact reviewed decision anchor to closure, stale behavior is proven, and a real distinct native review is represented by rehashed safe evidence rather than fixture claims.
</success_criteria>

<output>
Create `.planning/phases/05-acceptance-and-v1-0-0-release/05-01-SUMMARY.md` when done.
</output>
